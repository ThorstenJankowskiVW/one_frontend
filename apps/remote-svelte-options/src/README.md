# Svelte Flight Options Remote

`SvelteFlightOptions.svelte` ist die echte Svelte-Remote für Schritt 3 der Flight-Booking-Journey.
Vite kompiliert sie über `@sveltejs/vite-plugin-svelte`; der Einstiegspunkt ist
`target-svelte.js`, die eigene HTML-Seite `target-svelte.html`.
