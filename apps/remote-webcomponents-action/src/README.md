# Remote Web Components Action

Dieses Modul enthaelt `onefe-action-remote` als Custom Element.

Die Registrierung prueft `customElements.get(...)`, damit derselbe Tag-Name im Prototyp nicht doppelt registriert wird.
