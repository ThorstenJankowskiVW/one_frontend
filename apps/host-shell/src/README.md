# Host Shell

Zielordner fuer die React-basierte Host-/Shell-App.

Im ersten lokalen Scaffolding wird die lauffaehige Shell aus dem Repo-Root ueber `index.html`, `styles.css` und `app.js` bereitgestellt. Dieser Ordner bleibt fuer den naechsten Ausbau zu einer dedizierten React-App vorbereitet.
