import { ComponentInterface, EventEmitter } from '../../stencil-public-runtime';
export declare class Tabs implements ComponentInterface {
    /**
     * The id of the currently active tab.
     */
    activeTabId: string;
    /**
     * Tabs should cover the whole width
     */
    readonly fullWidth: boolean;
    /**
     * Tabs use spacing instead of padding
     */
    readonly noPadding: boolean;
    /**
     * dispatched when tab change has finished  => 'event.detail' delivers the current and previous tab
     */
    groupuiTabChange: EventEmitter<{
        tab: string;
        previousTab: string;
    }>;
    /**
     * dispatched when component finished loading
     */
    groupuiDidLoad: EventEmitter<void>;
    host: HTMLElement;
    activeTabPanel: HTMLElement;
    private titleObserver;
    private activeTabPanelIndex;
    private isSwitchingTabPanel;
    private previousTabPanel?;
    watchActiveTabHandler(newActiveTabId: string, previousActiveTabId: string): void;
    onTabChanged(): void;
    componentWillLoad(): void;
    componentDidLoad(): void;
    disconnectedCallback(): void;
    private setMutationsObserver;
    private initActiveTabPanel;
    private findIndexOfActiveTabOnStartup;
    private setActiveTabPanel;
    private getTabPanels;
    private getTabPanelsWithoutDisabled;
    private getNextSelectableTabPanel;
    private getPreviousSelectableTabPanel;
    /**
     * Select a tab by the value of its `tab` property or an element reference.
     *
     * @param tab The tab instance to select. If passed a string, it should be the value of the tab's `tab` property.
     */
    select(tab: string | HTMLElement): Promise<boolean>;
    private selectActiveTab;
    private getTabPanelIndex;
    private switchTabPanel;
    private handlePreviousTabPanel;
    /**
     * Get the currently selected tab.
     */
    getSelected(): Promise<string | undefined>;
    onKeyDown(event: KeyboardEvent): void;
    private selectFirstTabPanel;
    private selectLastTabPanel;
    private setTabIndex;
    private getTabTitles;
    private setFocus;
    private getTabBar;
    render(): any;
}
