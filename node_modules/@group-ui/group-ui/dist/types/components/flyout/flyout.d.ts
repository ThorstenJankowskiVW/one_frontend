import { EventEmitter } from '../../stencil-public-runtime';
import { FlyoutPosition } from '../../types';
/**
 * @slot default - default slot for content of the flyout
 */
export declare class Flyout {
    /**
     * toggles visibility
     */
    visible: boolean;
    /**
     * position of the flyout
     */
    readonly position: FlyoutPosition;
    /**
     * disables closing the flyout when clicked on backdrop
     */
    readonly disableBackdropClose: boolean;
    /**
     * disables closing the flyout on escape key press
     */
    readonly disableEscapeClose: boolean;
    host: HTMLElement;
    /**
     * is dispatched when flyout is closed
     */
    groupuiFlyoutClose: EventEmitter<void>;
    private dialogRef;
    private contentRef;
    componentDidLoad(): void;
    onBackdropClick(event: any): void;
    onEscapeKeyDown(event: KeyboardEvent): void;
    onDisplayedChanged(): void;
    private showDialog;
    private closeDialog;
    private handleClickOnBackdrop;
    private isScrollbarClick;
    private handleFocus;
    private getFocusableElements;
    private getAutoFocusElements;
    render(): any;
}
