import { EventEmitter } from '../../stencil-public-runtime';
import { InlineNotificationSeverity } from '../../types';
/**
 @slot headline - named slot 'headline', optional, made for text, but HTML works as well.
 @slot content - named slot 'content', optional, made for text, but HTML works as well.
 */
export declare class InlineNotification {
    /**
     * Severity of the inline notification
     */
    readonly severity: InlineNotificationSeverity;
    /**
     * Hides severity icon
     */
    readonly hideIcon: boolean;
    /**
     * Hides the close button that dismisses the inline notification
     */
    readonly hideCloseButton: boolean;
    /**
     * Hides inline-notification after given amount of milliseconds and emits groupuiClose event.
     */
    readonly autoDismiss: number;
    /**
     * Determines if the inline-notification is visible or not.
     */
    visible: boolean;
    /**
     * sets a custom accessible text for the close button
     */
    readonly a11yLabelCloseButton: string;
    /**
     * sets a custom accessible text for the severity icon (by default, it is hidden)
     */
    readonly a11yLabelSeverityIcon: string;
    /**
     * Is dispatched when the inline-notification is closed through click and/or autoDismiss.
     *
     * Hint: The inline-notification itself just sets its CSS display value to 'none'.
     * Use this event to e.g. remove element from the DOM inside your application.
     */
    groupuiClose: EventEmitter<void>;
    /**
     * Dispatched when component finished loading
     */
    groupuiDidLoad: EventEmitter<void>;
    host: HTMLElement;
    private timeoutId;
    componentWillLoad(): void;
    componentDidLoad(): void;
    onVisibleChanged(): void;
    onAutoDismissChanged(): void;
    private removeInitialAnimationDurationOverride;
    private handleVisibilityAndDismissal;
    private getFadeoutDuration;
    private clearTimeoutIfExist;
    private initiateAutoDismiss;
    private isHeadlineSlotProvided;
    private isContentSlotProvided;
    disconnectedCallback(): void;
    render(): any;
    private handleClose;
    private handleKeyDown;
}
