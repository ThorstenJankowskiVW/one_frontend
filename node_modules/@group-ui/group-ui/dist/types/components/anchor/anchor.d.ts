import { ComponentInterface } from '../../stencil-public-runtime';
/**
 * @slot default - default slot for anchor items
 * @slot label - named slot 'label' for the anchor label
 */
export declare class Anchor implements ComponentInterface {
    host: HTMLElement;
    /**
     * Reference ID of the scroll container
     */
    readonly scrollContainerIdRef: string;
    private intersectionsObserver;
    private destroy;
    private intersection;
    private click;
    private scroll;
    private scrollEnd;
    private scrollPositions;
    componentDidLoad(): void;
    private listenToScrollEvents;
    private listenToManualScroll;
    connectedCallback(): void;
    disconnectedCallback(): void;
    private isLabelSlotProvided;
    private referencedAnchorElements;
    private referencedAnchorElement;
    private focusFirstAnchorElement;
    private focusAnchorElement;
    private previousReferencedAnchorElement;
    private listenToAnchorItemClicks;
    private handleClickSideEffects;
    /**
     * Custom Operator: Waits for scrolling to fully settle.
     * If no scroll event fires within 100ms, proceeds immediately (no scrolling needed).
     * Otherwise, waits for 'scrollend' and then ensures that no intersection events
     * fire for 200ms before continuing.
     */
    private waitUntilScrollingSettled;
    private timerWon;
    private noScrollNeeded;
    /**
     * Waits for 'scrollend' and then waits for 200ms
     * without any further intersection events firing.
     */
    private waitForScrollEndAndIntersections;
    /**
     * Waits until no intersection events have fired for 200ms.
     */
    private waitForIntersectionsToSettle;
    private scrollContainer;
    private createIntersectionObservers;
    private referencedElementIsEnteringTopEdge;
    private referencedElementIsLeavingTopEdge;
    private anchorItemForId;
    private anchorItems;
    render(): any;
}
