import { EventEmitter } from '../../stencil-public-runtime';
import { SliderThumbSize } from '../../types';
/**
 * @nativeEvent change - dispatched when the value has changed => 'event.target.value' delivers the value of the slider
 *
 * @slot label - named slot 'label', here is any HTML content allowed.
 * @slot description - named slot 'description', here is any HTML content allowed.
 */
export declare class Slider {
    /**
     * minimum value
     */
    readonly min: number;
    /**
     * maximum value
     */
    readonly max: number;
    /**
     * size of the thumb
     */
    readonly thumbSize: SliderThumbSize;
    /**
     * step size. minimum 0.01
     */
    readonly step: number;
    /**
     * selected value
     */
    value: number;
    /**
     * sets slider to readonly
     */
    readonly disabled: boolean;
    /**
     * name of the input element to support forms
     */
    readonly name: string;
    /**
     * activates tick marks
     */
    readonly showTickMarks: boolean;
    /**
     * activates marks underneath the bar. will deactivate showTickMarks
     */
    readonly marks: boolean;
    /**
     * adds a suffix text to the labels, which are activated with the property marks
     */
    readonly suffix: string;
    /**
     * custom value range (numbers only, separated by semicolon)
     * overrides min, max and step
     */
    readonly customRange: string;
    /**
     * activates tooltip on drag
     */
    readonly showDragTooltip: boolean;
    /**
     * custom template for displaying the current value.
     * Use '{value}' as a placeholder, i.e. '{value}%' for 50%
     */
    readonly valueTemplate: string;
    /**
     * for react user only - use this instead of the native change event
     */
    groupuiChange: EventEmitter<void>;
    /**
     * dispatched when component finished loading
     */
    groupuiDidLoad: EventEmitter<void>;
    private isDragged;
    private displayedValue;
    host: HTMLElement;
    onKeyDown(event: KeyboardEvent): void;
    onDragStart(e: any): void;
    onDragEnd(): void;
    onDrag(e: any): void;
    onDraggedChanged(): void;
    onValueChanged(): void;
    componentDidLoad(): void;
    private addAriaLabel;
    private decreaseOneStep;
    private increaseOneStep;
    private getMinimum;
    private getMaximum;
    private parseCustomRange;
    private isTrackPressed;
    private isThumbPressed;
    private hideTooltip;
    private showTooltip;
    private displayValue;
    private moveThumb;
    private moveTooltip;
    private adjustFill;
    private constrained;
    private rounded;
    private onSliderMoved;
    private roundToNearestStep;
    private trackPositionToValue;
    private fillRatio;
    private fillPercentage;
    private percentageOnTrack;
    private getSlotContent;
    private addFormsHandling;
    private tooltip;
    private thumb;
    private track;
    private fill;
    private isLabelSlotProvided;
    private isDescriptionSlotProvided;
    private tickMarkCount;
    private tickMarks;
    private markers;
    private getTickPosition;
    private getAriaValueText;
    private isTopInfoAvailable;
    private filledValueTemplate;
    private static browserLanguage;
    render(): any;
}
