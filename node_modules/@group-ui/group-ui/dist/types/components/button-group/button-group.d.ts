import { ComponentInterface, EventEmitter } from '../../stencil-public-runtime';
import { ButtonGroupChangeEventDetails, ButtonGroupIconPosition, ButtonGroupMode, ButtonGroupSize, ButtonGroupVariant } from '../../types';
/**
 * @nativeEvent click  - dispatched when button was clicked => 'event.target.value' delivers the value of the clicked button
 * @nativeEvent change - dispatched when the selection has changed => 'event.detail' delivers the current selection state as an object with value (value) and content (innerText)
 * */
export declare class ButtonGroup implements ComponentInterface {
    private static readonly CSS_VALUE_UNIT_REGEX;
    host: HTMLElement;
    /**
     * activates an inverted presentation of the group and the nested buttons
     */
    readonly inverted: boolean;
    /**
     * sets the button-group to fullwidth and every child to grow with same width
     */
    readonly fullwidth: boolean;
    /**
     * mode of the button-group
     */
    readonly mode?: ButtonGroupMode;
    /**
     * variant of the button-group
     */
    readonly variant: ButtonGroupVariant;
    /**
     * deactivates the button-group
     */
    readonly disabled: boolean;
    /**
     * used to be referenced in a form
     */
    readonly name: string;
    /**
     * sets a gap between the individual buttons
     */
    readonly gap: string;
    /**
     * sets the size
     */
    readonly size: ButtonGroupSize;
    /**
     * sets the icon position
     */
    readonly iconPosition: ButtonGroupIconPosition;
    /**
     * sets icon only mode
     */
    readonly iconOnly: boolean;
    /**
     * dispatched when component finished loading
     */
    groupuiDidLoad: EventEmitter<void>;
    /**
     * for react user only - is dispatched when the checked state is changed
     */
    groupuiChange: EventEmitter<ButtonGroupChangeEventDetails>;
    updateDisabledPropOfChild(): void;
    updateInvertedPropOfChild(): void;
    updateFullwidthPropOfChild(): void;
    updateSizePropOfChild(): void;
    updateVariantPropOfChild(): void;
    updateIconPositionPropOfChild(): void;
    updateIconOnlyPropOfChild(): void;
    onKeyDown(event: KeyboardEvent): void;
    handleClick(event: Event): void;
    private initializeButtonGroupButtons;
    private toggleCheckbox;
    private selectRadioButton;
    private uncheckAll;
    private changeEvent;
    private changeEventPayload;
    private checkedButtons;
    componentWillRender(): void;
    private updatePositionProperty;
    onGapChanged(): void;
    private onSlotChange;
    private updateGapProperty;
    private updateSpacedProperty;
    componentWillLoad(): void;
    private buttonGroupButtons;
    private waitForHydrated;
    private setRoles;
    componentDidLoad(): Promise<void>;
    render(): any;
}
