import { ComponentInterface, EventEmitter } from '../../../../stencil-public-runtime';
import { NavigationSizes } from '../../../../types';
/**
 * @slot default - default slot for the content of the burger-menu component (usually groupui-burger-menu-items)
 */
export declare class BurgerMenu implements ComponentInterface {
    /**
     * toggles the menu
     */
    open: boolean;
    /**
     * sets the side-navigation size
     */
    readonly size: NavigationSizes;
    /**
     * disables the slide animation
     */
    readonly noAnimation: boolean;
    /**
     * activates the dividers
     */
    readonly divider: boolean;
    /**
     * moves the chevrons to the left side
     */
    readonly treeLayout: boolean;
    /**
     * ARIA label for the open button.
     */
    readonly a11yLabelOpenButton: string;
    /**
     * ARIA label for the close button.
     */
    readonly a11yLabelCloseButton: string;
    /**
     * Emitted when the component has loaded.
     */
    groupuiDidLoad: EventEmitter<void>;
    /**
     * Emitted when the active item changes
     */
    groupuiChange: EventEmitter<void>;
    host: HTMLElement;
    private ariaAttributes;
    private navigationUtilities;
    componentWillLoad(): void;
    updateSizePropOfChild(): void;
    updateDividerPropOfChild(): void;
    updateTreeLayoutPropOfChild(): void;
    onOpenChange(): void;
    handleClick(event: Event): void;
    handleKeyDown(event: KeyboardEvent): void;
    private preventScrolling;
    private handleTabOrder;
    private disableKeyboardHandlingForItems;
    private enableKeyboardHandlingForItems;
    private closeDrilldowns;
    private toggleOpen;
    private iconName;
    private getButtonAriaLabel;
    render(): any;
}
