import { ComponentInterface, EventEmitter } from '../../../stencil-public-runtime';
import { Breakpoint } from '../../../types';
/**
 * @slot default - default slot for the navigation items
 */
export declare class GlobalTopNavigation implements ComponentInterface {
    /**
     * Breakpoint on which the layout switches to mobile.
     */
    readonly breakpoint: Breakpoint;
    /**
     * Custom breakpoint in pixels to switch to mobile layout.
     * Overwrites the `breakpoint` property.
     */
    readonly customBreakpoint: number;
    /**
     * Deactivates the active state management.
     */
    readonly manualMode: boolean;
    /**
     * Emitted when the component has loaded.
     */
    groupuiDidLoad: EventEmitter<void>;
    /**
     * Emitted when the active item changes
     */
    groupuiChange: EventEmitter<void>;
    private itemElements;
    private activeItemIndex;
    private ariaAttributes;
    isMobileLayoutVisible: boolean;
    host: HTMLElement;
    private resizeObserver;
    private menuObserver;
    connectedCallback(): void;
    componentWillLoad(): void;
    componentDidLoad(): void;
    disconnectedCallback(): void;
    updateManualModeOfBurger(): void;
    private createResizeObserver;
    private observeResize;
    private updateItems;
    private removeItemsInsidePopovers;
    private removeItemsInsideMenus;
    private setActiveItem;
    handleClick(event: Event): void;
    private updateSlotChangeEventListener;
    private handleSlotChange;
    private assignBrandLogoIfPresent;
    private hasUtilityItems;
    private hasMobileUtilityItems;
    private hasNavItems;
    private hasMobileNavItems;
    render(): any;
}
