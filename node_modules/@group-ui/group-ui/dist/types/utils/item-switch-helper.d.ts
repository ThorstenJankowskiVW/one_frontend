import { EventEmitter } from '../stencil-public-runtime';
export interface ItemSwitch {
    item: HTMLElement;
    itemTag: string;
    host: HTMLElement;
    attribute: string;
    event?: EventEmitter;
}
export declare class ItemSwitchHelper {
    static switchItem(itemSwitch: ItemSwitch): void;
    private static resetItemState;
}
