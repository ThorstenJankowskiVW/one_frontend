import { IconSize } from '../types';
export interface OperatingParameters {
    host: HTMLElement;
    size: IconSize;
    selector: string;
}
export declare class IconSizeHelper {
    private static ICON_SIZE_SMALL;
    private static ICON_SIZE_MEDIUM;
    private static ICON_SIZE_LARGE;
    private static getSize;
    static setHeightAndWidth(parameters: OperatingParameters): void;
}
