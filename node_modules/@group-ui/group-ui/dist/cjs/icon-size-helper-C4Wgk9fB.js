'use strict';

class IconSizeHelper {
    static getSize(size) {
        switch (size) {
            case 's':
                return IconSizeHelper.ICON_SIZE_SMALL;
            case 'm':
                return IconSizeHelper.ICON_SIZE_MEDIUM;
            case 'l':
            default:
                return IconSizeHelper.ICON_SIZE_LARGE;
        }
    }
    static setHeightAndWidth(parameters) {
        const icon = parameters.host.shadowRoot.querySelector(parameters.selector);
        if (icon) {
            const newSize = this.getSize(parameters.size);
            icon.height = newSize;
            icon.width = newSize;
        }
    }
}
IconSizeHelper.ICON_SIZE_SMALL = 16;
IconSizeHelper.ICON_SIZE_MEDIUM = 24;
IconSizeHelper.ICON_SIZE_LARGE = 32;

exports.IconSizeHelper = IconSizeHelper;
//# sourceMappingURL=icon-size-helper-C4Wgk9fB.js.map

//# sourceMappingURL=icon-size-helper-C4Wgk9fB.js.map