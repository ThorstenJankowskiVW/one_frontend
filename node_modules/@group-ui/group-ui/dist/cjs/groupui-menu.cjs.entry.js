'use strict';

var index = require('./index-C35xP9d6.js');
var index$1 = require('./index-Dssn3hdS.js');
var focusHelper = require('./focus-helper-BbK4CMdH.js');

const menuCss = () => `:host #menu-items:focus{outline:none}`;

const Menu = class {
    constructor(hostRef) {
        index.registerInstance(this, hostRef);
        /**
         * toggles visibility
         */
        this.visible = false;
        /**
         * set max-height for the container (e.g. "200px")
         */
        this.maxHeight = 'none';
        /**
         * content placement relative to the trigger element.
         * for a list of possible placements check https://popper.js.org/docs/v2/constructors/#options
         */
        this.placement = 'bottom-start';
        /**
         * content offset relative to the trigger element (in pixel)
         */
        this.offset = 0;
        /**
         * minimum width of the menu content (e.g. "200px")
         * if not set, the minWidth is determined by the content
         */
        this.minWidth = 'none';
        this.delayBeforeCloseOnHover = 160;
        this.delayBeforeOpenOnHover = 480;
        this.outsideClick = new index$1.cjsExports.Observable();
        this.defaultSlotChange = new index$1.cjsExports.Subject();
        this.menuGroupsSlotChanges = new index$1.cjsExports.Observable();
        this.destroy = new index$1.cjsExports.Subject();
        this.menuJustOpenedWithKeyboard = false;
        this.menuJustOpenedWithHover = false;
        this.handleOutsideClick = (event) => {
            if (this.visible && !event.composedPath().includes(this.host)) {
                this.visible = false;
            }
        };
        this.handleTriggerClick = () => {
            if (this.isSubmenu()) {
                this.visible = true;
            }
            else {
                this.visible = !this.visible;
            }
        };
    }
    onVisibleChange() {
        this.updateTriggerElementForParentMenu();
        if (this.visible && this.isSubmenu()) {
            this.closeAllSiblingMenus();
            return;
        }
        if (!this.visible && this.isSubmenu()) {
            this.focusMenuItemsContainerOfParentMenu();
        }
    }
    updateTriggerElementForParentMenu() {
        var _a, _b;
        if (!this.isSubmenu())
            return;
        if (this.visible) {
            (_a = this.triggerElementForParentMenu()) === null || _a === void 0 ? void 0 : _a.classList.add('hover');
        }
        else {
            (_b = this.triggerElementForParentMenu()) === null || _b === void 0 ? void 0 : _b.classList.remove('hover');
        }
    }
    triggerElementForParentMenu() {
        return this.host.querySelector(':scope > groupui-menu-item[slot="trigger"]');
    }
    componentDidLoad() {
        this.createDefaultSlotChangeListener();
        this.createMenuGroupSlotChangeListener();
        this.subscribeToTriggerClick();
        this.subscribeToOutsideClicks();
        this.subscribeToKeydownEvents();
        this.subscribeToDefaultSlotChange();
        this.subscribeToCombinedSlotChanges();
        if (this.isSubmenu()) {
            this.subscribeToTriggerOpenKeyDowns();
            this.subscribeToTriggerHover();
            this.subscribeToMenuMouseLeave();
        }
        this.updateFirstItem();
        this.updateLastItem();
        this.updateTriggerElementForParentMenu();
    }
    createDefaultSlotChangeListener() {
        index$1.cjsExports.fromEvent(this.defaultSlot(), 'slotchange')
            .pipe(index$1.cjsExports.takeUntil(this.destroy))
            .subscribe(() => {
            this.defaultSlotChange.next();
        });
    }
    subscribeToDefaultSlotChange() {
        this.defaultSlotChange.pipe(index$1.cjsExports.takeUntil(this.destroy)).subscribe(() => {
            this.createMenuGroupSlotChangeListener();
            this.subscribeToCombinedSlotChanges();
        });
    }
    createMenuGroupSlotChangeListener() {
        this.menuGroupsSlotChanges = index$1.cjsExports.merge(...this.menuGroups().map((group) => {
            return index$1.cjsExports.fromEvent(group, 'MenuGroupOnSlotChange');
        })).pipe(index$1.cjsExports.takeUntil(index$1.cjsExports.merge(this.destroy, this.defaultSlotChange)));
    }
    subscribeToCombinedSlotChanges() {
        index$1.cjsExports.merge(this.menuGroupsSlotChanges, this.defaultSlotChange)
            .pipe(index$1.cjsExports.takeUntil(this.destroy))
            .subscribe(() => {
            this.resetFirstAndLastItem();
            this.updateFirstItem();
            this.updateLastItem();
        });
    }
    resetFirstAndLastItem() {
        this.menuItems().forEach((item) => {
            item.classList.remove('first');
            item.classList.remove('last');
        });
    }
    updateFirstItem() {
        const menuItems = this.menuItems();
        if (menuItems.length === 0)
            return;
        const first = menuItems[0];
        if (this.itemIsInGroupWithoutLabel(first)) {
            first.classList.add('first');
        }
    }
    itemIsInGroupWithoutLabel(item) {
        const group = item.closest('groupui-menu-group');
        return group === null || group.shadowRoot.querySelector('slot[name="label"]') === null;
    }
    updateLastItem() {
        const menuItems = this.menuItems();
        if (menuItems.length === 0)
            return;
        const last = menuItems[menuItems.length - 1];
        last.classList.add('last');
    }
    subscribeToKeydownEvents() {
        index$1.cjsExports.fromEvent(this.host, 'keydown')
            .pipe(index$1.cjsExports.filter(() => this.visible))
            .pipe(index$1.cjsExports.takeUntil(this.destroy))
            .subscribe((event) => {
            this.handleKeyDown(event);
        });
    }
    disconnectedCallback() {
        this.destroy.next();
        this.destroy.complete();
    }
    closeAllSiblingMenus() {
        const parentMenu = this.host.parentElement.closest('groupui-menu');
        Array.from(parentMenu.querySelectorAll('groupui-menu')).forEach((menu) => {
            menu.removeAttribute('visible');
        });
    }
    handleTriggerOpenKeyDown() {
        if (this.visible && this.isSubmenu()) {
            this.firstMenuItem().focus();
            return;
        }
        this.visible = true;
        this.menuJustOpenedWithKeyboard = true;
    }
    componentDidRender() {
        if (this.menuJustOpenedWithKeyboard && this.isSubmenu()) {
            this.firstMenuItem().focus();
            this.menuJustOpenedWithKeyboard = false;
        }
        if (this.menuJustOpenedWithHover && this.isSubmenu()) {
            this.menuItemsContainer().focus();
            this.menuJustOpenedWithHover = false;
        }
    }
    menuItemsContainer() {
        return this.host.shadowRoot.querySelector('#menu-items');
    }
    focusMenuItemsContainerOfParentMenu() {
        const parentMenu = this.host.parentElement.closest('groupui-menu');
        (parentMenu === null || parentMenu === void 0 ? void 0 : parentMenu.shadowRoot.querySelector('#menu-items')).focus();
    }
    subscribeToOutsideClicks() {
        this.outsideClick = index$1.cjsExports.fromEvent(document, 'click').pipe(index$1.cjsExports.takeUntil(this.destroy));
        this.outsideClick.subscribe((event) => {
            this.handleOutsideClick(event);
        });
    }
    subscribeToMenuMouseLeave() {
        const mouseenter = index$1.cjsExports.fromEvent(this.host, 'mouseenter');
        const mouseleave = index$1.cjsExports.fromEvent(this.host, 'mouseleave');
        const fire = mouseleave.pipe(index$1.cjsExports.switchMap(() => {
            return index$1.cjsExports.timer(this.delayBeforeCloseOnHover).pipe(index$1.cjsExports.takeUntil(index$1.cjsExports.merge(mouseenter, this.destroy)));
        }));
        fire.subscribe(() => {
            this.visible = false;
        });
    }
    subscribeToTriggerHover() {
        const mouseenter = index$1.cjsExports.fromEvent(this.triggerSlot(), 'mouseenter');
        const mouseleave = index$1.cjsExports.fromEvent(this.triggerSlot(), 'mouseleave');
        const longHover = mouseenter
            .pipe(index$1.cjsExports.switchMap(() => {
            return index$1.cjsExports.timer(this.delayBeforeOpenOnHover).pipe(index$1.cjsExports.takeUntil(index$1.cjsExports.merge(mouseleave, this.destroy)));
        }))
            .pipe(index$1.cjsExports.takeUntil(this.destroy));
        longHover.subscribe(() => {
            this.menuJustOpenedWithHover = true;
            this.visible = true;
        });
    }
    subscribeToTriggerClick() {
        const triggerClick = index$1.cjsExports.fromEvent(this.triggerSlot(), 'click');
        triggerClick.pipe(index$1.cjsExports.takeUntil(this.destroy)).subscribe(() => {
            this.handleTriggerClick();
        });
    }
    subscribeToTriggerOpenKeyDowns() {
        const arrowRight = index$1.cjsExports.fromEvent(this.triggerSlot(), 'keydown').pipe(index$1.cjsExports.filter((event) => event.key === 'ArrowRight' || event.key === 'Enter' || event.key === ' '));
        arrowRight.pipe(index$1.cjsExports.takeUntil(this.destroy)).subscribe(() => {
            this.handleTriggerOpenKeyDown();
        });
    }
    triggerSlot() {
        return this.host.shadowRoot.querySelector('[name="trigger"]');
    }
    triggerElement() {
        return this.host.querySelector('[slot="trigger"]');
    }
    menuItems() {
        return Array.from(this.host.querySelectorAll(':scope > groupui-menu-item:not([slot="trigger"]), :scope > groupui-menu-group > groupui-menu-item, :scope > groupui-menu > groupui-menu-item[slot="trigger"], :scope > groupui-menu-group > groupui-menu > groupui-menu-item[slot="trigger"]'));
    }
    menuGroups() {
        return Array.from(this.host.children).filter((element) => {
            return element.tagName === 'GROUPUI-MENU-GROUP';
        });
    }
    firstMenuItem() {
        const items = this.menuItems();
        if (items.length > 0) {
            return items[0];
        }
        return null;
    }
    lastMenuItem() {
        const items = this.menuItems();
        if (items.length > 0) {
            return items[items.length - 1];
        }
        return null;
    }
    previousMenuItem() {
        const items = this.menuItems();
        const activeItemIndex = items.findIndex((item) => item === document.activeElement);
        if (activeItemIndex === -1 || activeItemIndex === 0) {
            return this.lastMenuItem();
        }
        return items[(activeItemIndex - 1) % items.length];
    }
    nextMenuItem() {
        const items = this.menuItems();
        const activeItemIndex = items.findIndex((item) => item === document.activeElement);
        if (activeItemIndex === -1) {
            return this.firstMenuItem();
        }
        return items[(activeItemIndex + 1) % items.length];
    }
    handleKeyDown(event) {
        var _a, _b, _c, _d, _e;
        if (event.key === 'ArrowDown') {
            event.preventDefault();
            event.stopPropagation();
            (_a = this.nextMenuItem()) === null || _a === void 0 ? void 0 : _a.focus();
            return;
        }
        if (event.key === 'ArrowUp') {
            event.preventDefault();
            event.stopPropagation();
            (_b = this.previousMenuItem()) === null || _b === void 0 ? void 0 : _b.focus();
            return;
        }
        if (event.key === 'Home') {
            event.preventDefault();
            event.stopPropagation();
            (_c = this.firstMenuItem()) === null || _c === void 0 ? void 0 : _c.focus();
            return;
        }
        if (event.key === 'End') {
            event.preventDefault();
            event.stopPropagation();
            (_d = this.lastMenuItem()) === null || _d === void 0 ? void 0 : _d.focus();
            return;
        }
        if (event.key === 'ArrowLeft') {
            if (this.isSubmenu()) {
                event.preventDefault();
                event.stopPropagation();
                this.visible = false;
                (_e = this.triggerElementForParentMenu()) === null || _e === void 0 ? void 0 : _e.focus();
            }
            return;
        }
        if (event.key === 'Escape') {
            this.triggerElement().focus();
            this.visible = false;
        }
        if (event.key === 'Enter') {
            const eventFromMenuGroup = event.target.closest('groupui-menu-group:not([mode="multiple"])');
            const isSubmenuTrigger = event.target.getAttribute('slot') === 'trigger';
            if (eventFromMenuGroup && !isSubmenuTrigger) {
                this.triggerElement().focus();
                this.visible = false;
            }
        }
        if (event.key === 'Tab') {
            event.preventDefault();
            this.visible = false;
            if (event.shiftKey) {
                const previousFocusableElement = focusHelper.FocusHelper.getPreviousFocusableElement(this.host);
                previousFocusableElement && previousFocusableElement.focus && previousFocusableElement.focus();
            }
            else {
                const nextFocusableElement = focusHelper.FocusHelper.getNextFocusableElement(this.host);
                nextFocusableElement && nextFocusableElement.focus && nextFocusableElement.focus();
            }
        }
    }
    defaultSlot() {
        return this.host.shadowRoot.querySelector('slot:not([name])');
    }
    isSubmenu() {
        return this.host.parentElement.closest('groupui-menu') !== null;
    }
    getStyle() {
        const minWidth = this.minWidth ? { minWidth: this.minWidth } : {};
        const maxHeight = this.maxHeight ? { maxHeight: this.maxHeight } : {};
        return { ...minWidth, ...maxHeight };
    }
    render() {
        return (index.h(index.Host, { key: '943a37894c38296941a1498d81a2f74d96184ff5', class: { submenu: this.isSubmenu() } }, index.h("groupui-popover", { key: 'c12761dbfbf2b7a929e7ce4aeab125d885de587a', visible: this.visible, "full-width": this.fullWidth, offset: this.offset, placement: this.isSubmenu() ? 'right-start' : this.placement }, index.h("slot", { key: '630c305d5e61eeac4d2c10a8e18fb597becdef0f', slot: "parent", name: "trigger" }), index.h("div", { key: '894d75ca06350a73d41ea739a147495a553096b7', tabIndex: -1, slot: "content", id: "menu-items", style: this.getStyle(), role: "menu", "aria-expanded": this.visible ? 'true' : 'false' }, index.h("slot", { key: '8e17975e11b42bd511339f19759f60bd69984b90' })))));
    }
    get host() { return index.getElement(this); }
    static get watchers() { return {
        "visible": [{
                "onVisibleChange": 0
            }]
    }; }
};
Menu.style = menuCss();

exports.groupui_menu = Menu;
//# sourceMappingURL=groupui-menu.entry.cjs.js.map

//# sourceMappingURL=groupui-menu.cjs.entry.js.map