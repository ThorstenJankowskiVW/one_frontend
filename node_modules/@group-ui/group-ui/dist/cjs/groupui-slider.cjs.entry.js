'use strict';

var index = require('./index-C35xP9d6.js');

const sliderCss = () => `:host{display:-ms-flexbox;display:flex;-ms-flex-align:start;align-items:start;-ms-flex-direction:column;flex-direction:column;font-family:var(--groupui-sys-font-family-primary), sans-serif;font-size:var(--groupui-sys-font-size-350);line-height:var(--groupui-sys-line-height-xxl);letter-spacing:var(--groupui-sys-letter-spacing-regular);font-weight:var(--groupui-sys-font-weight-normal);outline:var(--groupui-sys-border-width-none);color:var(--groupui-sys-color-text-normal)}:host .top-info{display:-ms-flexbox;display:flex;width:100%;margin-bottom:var(--groupui-sys-spacing-300)}:host .top-info .label{font-weight:var(--groupui-sys-font-weight-bold)}:host .description{margin-top:var(--groupui-sys-spacing-300)}:host .description.with-marks{margin-top:var(--groupui-sys-spacing-1200)}:host .value{margin-left:auto}:host .track{cursor:pointer;-webkit-user-select:none;-moz-user-select:none;-ms-user-select:none;user-select:none;width:100%;padding-block:calc(var(--groupui-sys-spacing-150) + var(--groupui-sys-spacing-100))}:host .track .bar{position:relative;width:100%;height:var(--groupui-sys-spacing-100);border-radius:var(--groupui-sys-border-radius-full);background-color:var(--groupui-sys-color-action-quiet-disabled)}:host .track .bar .tick-mark{position:absolute;width:var(--groupui-sys-spacing-100);height:var(--groupui-sys-spacing-100);border-radius:var(--groupui-sys-border-radius-full);background-color:var(--groupui-sys-color-action-basic-disabled);-webkit-transform:translateX(-50%);transform:translateX(-50%)}:host .track .bar .mark{position:absolute;top:var(--groupui-sys-spacing-600);width:var(--groupui-sys-spacing-0);height:var(--groupui-sys-spacing-150);border:var(--groupui-sys-border-width-s) var(--groupui-sys-stroke-style-primary) var(--groupui-sys-color-action-basic-disabled);border-radius:var(--groupui-sys-border-width-s);-webkit-transform:translateX(0%);transform:translateX(0%)}:host .track .bar .mark~.mark{-webkit-transform:translateX(-50%);transform:translateX(-50%)}:host .track .bar .mark:nth-last-child(2){-webkit-transform:translateX(-100%);transform:translateX(-100%)}:host .track .bar .label{position:absolute;top:var(--groupui-sys-spacing-1000);font-size:var(--groupui-sys-font-size-350);-webkit-transform:translateX(0%);transform:translateX(0%);white-space:nowrap}:host .track .bar .label~.label{-webkit-transform:translateX(-50%);transform:translateX(-50%)}:host .track .bar .label:last-child{-webkit-transform:translateX(-100%);transform:translateX(-100%)}:host .track .bar .fill{position:absolute;left:var(--groupui-sys-spacing-0);right:100%;top:50%;-webkit-transform:translateY(-50%);transform:translateY(-50%);height:var(--groupui-sys-spacing-100);border-radius:var(--groupui-sys-border-radius-full);background-color:var(--groupui-sys-color-action-basic-default)}:host .track .bar .thumb{position:absolute;border-radius:var(--groupui-sys-border-radius-full);left:var(--groupui-sys-spacing-0);top:50%;height:var(--groupui-sys-spacing-600);width:var(--groupui-sys-spacing-600);-webkit-transform:translate(-50%, -50%);transform:translate(-50%, -50%);outline:var(--groupui-sys-spacing-200) var(--groupui-sys-stroke-style-primary) transparent;background-color:var(--groupui-sys-color-action-basic-default);-webkit-transition:outline-color var(--groupui-sys-duration-medium-2) var(--groupui-sys-cubic-bezier-ease-in-out);transition:outline-color var(--groupui-sys-duration-medium-2) var(--groupui-sys-cubic-bezier-ease-in-out)}:host .track .bar .thumb:hover{outline-color:var(--groupui-sys-color-action-subtle-hover)}:host .track .bar .thumb:active{outline-color:var(--groupui-sys-color-action-subtle-pressed)}:host .track .bar .tooltip{position:absolute;-webkit-box-sizing:border-box;box-sizing:border-box;opacity:var(--groupui-sys-opacity-0);-webkit-transform:translateX(-50%);transform:translateX(-50%);min-width:var(--groupui-sys-spacing-800);padding-inline:var(--groupui-sys-spacing-200);border-radius:var(--groupui-sys-border-radius-s);bottom:calc(100% + var(--groupui-sys-spacing-400) + var(--groupui-sys-spacing-50));-webkit-transition:opacity var(--groupui-sys-duration-medium-2) var(--groupui-sys-cubic-bezier-ease-in-out);transition:opacity var(--groupui-sys-duration-medium-2) var(--groupui-sys-cubic-bezier-ease-in-out);background-color:var(--groupui-sys-color-action-basic-default);white-space:nowrap}:host .track .bar .tooltip span{position:relative;display:block;text-align:center;line-height:var(--groupui-sys-font-size-800);color:var(--groupui-sys-color-on-action-basic-default)}:host .track .bar .tooltip span:before{position:absolute;content:"";left:50%;-webkit-transform:translateX(-50%) rotate(45deg);transform:translateX(-50%) rotate(45deg);bottom:calc(-1 * (var(--groupui-sys-spacing-25) + var(--groupui-sys-spacing-50)));width:var(--groupui-sys-spacing-200);height:var(--groupui-sys-spacing-200);background-color:var(--groupui-sys-color-action-basic-default)}:host .track:active .bar .thumb{outline-color:var(--groupui-sys-color-action-subtle-pressed)}:host([thumb-size=s]) .track .bar .thumb{height:var(--groupui-sys-spacing-400);width:var(--groupui-sys-spacing-400)}:host([thumb-size=m]) .track .bar .thumb{height:var(--groupui-sys-spacing-500);width:var(--groupui-sys-spacing-500)}:host([thumb-size=l]) .track .bar .thumb{height:var(--groupui-sys-spacing-600);width:var(--groupui-sys-spacing-600)}:host(:focus-visible) .track .bar .thumb{-webkit-box-shadow:0 0 0 var(--groupui-sys-border-width-m) var(--groupui-sys-color-background-base), 0 0 0 calc(2 * var(--groupui-sys-border-width-m)) var(--groupui-sys-color-action-basic-default);box-shadow:0 0 0 var(--groupui-sys-border-width-m) var(--groupui-sys-color-background-base), 0 0 0 calc(2 * var(--groupui-sys-border-width-m)) var(--groupui-sys-color-action-basic-default)}:host([disabled=""]) .track,:host([disabled=true]) .track{cursor:not-allowed}:host([disabled=""]) .track .bar,:host([disabled=true]) .track .bar{background-color:var(--groupui-sys-color-action-quiet-disabled)}:host([disabled=""]) .track .bar .fill,:host([disabled=true]) .track .bar .fill{background-color:var(--groupui-sys-color-action-basic-disabled)}:host([disabled=""]) .track .bar .thumb,:host([disabled=true]) .track .bar .thumb{outline:none;background-color:var(--groupui-sys-color-action-basic-disabled)}:host([disabled=""]) .description,:host([disabled=""]) .label,:host([disabled=""]) .value,:host([disabled=true]) .description,:host([disabled=true]) .label,:host([disabled=true]) .value{color:var(--groupui-sys-color-action-basic-disabled)}`;

const Slider = class {
    constructor(hostRef) {
        index.registerInstance(this, hostRef);
        this.groupuiChange = index.createEvent(this, "groupuiChange", 3);
        this.groupuiDidLoad = index.createEvent(this, "groupuiDidLoad", 3);
        /**
         * minimum value
         */
        this.min = 0;
        /**
         * maximum value
         */
        this.max = 100;
        /**
         * size of the thumb
         */
        this.thumbSize = 'l';
        /**
         * step size. minimum 0.01
         */
        this.step = 1;
        /**
         * selected value
         */
        this.value = this.min;
        /**
         * adds a suffix text to the labels, which are activated with the property marks
         */
        this.suffix = '';
        this.isDragged = false;
    }
    onKeyDown(event) {
        if (this.disabled)
            return;
        if (event.code === 'ArrowLeft' || event.code === 'ArrowDown') {
            this.value = this.decreaseOneStep();
            event.preventDefault();
            return;
        }
        if (event.code === 'ArrowRight' || event.code === 'ArrowUp') {
            this.value = this.increaseOneStep();
            event.preventDefault();
            return;
        }
        if (event.code === 'Home') {
            this.value = this.getMinimum();
            event.preventDefault();
            return;
        }
        if (event.code === 'End') {
            this.value = this.getMaximum();
            event.preventDefault();
        }
    }
    onDragStart(e) {
        if (this.disabled)
            return;
        if (this.isThumbPressed(e)) {
            this.isDragged = true;
            return;
        }
        if (this.isTrackPressed(e)) {
            this.onSliderMoved(e);
            this.isDragged = true;
        }
    }
    onDragEnd() {
        this.isDragged = false;
    }
    onDrag(e) {
        if (!this.isDragged)
            return;
        this.onSliderMoved(e);
    }
    onDraggedChanged() {
        if (this.isDragged) {
            if (this.showDragTooltip)
                this.showTooltip();
        }
        else {
            this.hideTooltip();
        }
    }
    onValueChanged() {
        this.displayValue();
        this.host.dispatchEvent(new window.Event('change'));
        this.groupuiChange.emit();
    }
    componentDidLoad() {
        this.displayValue();
        this.addFormsHandling();
        this.addAriaLabel();
        this.groupuiDidLoad.emit();
    }
    addAriaLabel() {
        if (this.isLabelSlotProvided()) {
            if (!this.host.hasAttribute('aria-label')) {
                this.host.setAttribute('aria-label', this.getSlotContent('label'));
            }
        }
    }
    decreaseOneStep() {
        return this.constrained(this.rounded(this.value - this.step));
    }
    increaseOneStep() {
        return this.constrained(this.rounded(this.value + this.step));
    }
    getMinimum() {
        if (this.customRange)
            return this.parseCustomRange()[0];
        return this.min;
    }
    getMaximum() {
        if (this.customRange)
            return this.parseCustomRange().pop();
        return this.max;
    }
    parseCustomRange() {
        if (!this.customRange)
            return [];
        return this.customRange
            .split(';')
            .map((element) => Number(element.trim()))
            .filter((element) => !isNaN(element));
    }
    isTrackPressed(e) {
        return e.composedPath().includes(this.track());
    }
    isThumbPressed(e) {
        return e.composedPath().includes(this.thumb());
    }
    hideTooltip() {
        this.tooltip().style.opacity = '0';
    }
    showTooltip() {
        this.tooltip().style.opacity = '1';
    }
    displayValue() {
        this.displayedValue = this.constrained(this.value);
        this.moveThumb();
        this.moveTooltip();
        this.adjustFill();
    }
    moveThumb() {
        const thumb = this.thumb();
        if (!thumb)
            return;
        thumb.style.left = `${this.fillPercentage()}%`;
    }
    moveTooltip() {
        const tooltip = this.tooltip();
        if (!tooltip)
            return;
        tooltip.style.left = `${this.fillPercentage()}%`;
    }
    adjustFill() {
        const fill = this.fill();
        if (!fill)
            return;
        fill.style.right = `${100 - this.fillPercentage()}%`;
    }
    constrained(value) {
        return Math.max(this.getMinimum(), Math.min(this.getMaximum(), value));
    }
    rounded(value) {
        return Math.round((value + Number.EPSILON) * 100) / 100;
    }
    onSliderMoved(event) {
        this.value = this.constrained(this.rounded(this.roundToNearestStep(this.trackPositionToValue(event.clientX))));
    }
    roundToNearestStep(value) {
        if (this.step === 0)
            return value;
        if (this.customRange) {
            let smallestDifference = Infinity;
            let closestStep = 0;
            this.parseCustomRange().forEach((step) => {
                if (Math.abs(value - step) < smallestDifference) {
                    smallestDifference = Math.abs(value - step);
                    closestStep = step;
                }
            });
            return closestStep;
        }
        return Math.round(value / this.step) * this.step;
    }
    trackPositionToValue(pos) {
        const ratioOfPosition = this.fillRatio(pos);
        if (this.customRange) {
            const range = this.parseCustomRange();
            for (let i = 0; i < range.length; i++) {
                const startPercentage = i / (range.length - 1);
                const endPercentage = (i + 1) / (range.length - 1);
                if (ratioOfPosition > 1)
                    return this.getMaximum();
                if (startPercentage <= ratioOfPosition && endPercentage >= ratioOfPosition) {
                    const relativeRatio = (ratioOfPosition - startPercentage) / (endPercentage - startPercentage);
                    return (range[i + 1] - range[i]) * relativeRatio + range[i];
                }
            }
        }
        return (this.max - this.min) * ratioOfPosition + this.min;
    }
    fillRatio(clampedPosition) {
        const track = this.track();
        if (!track)
            return 0;
        return ((clampedPosition - track.getBoundingClientRect().left) /
            (track.getBoundingClientRect().right - track.getBoundingClientRect().left));
    }
    fillPercentage() {
        return this.percentageOnTrack(this.displayedValue);
    }
    percentageOnTrack(value) {
        if (this.customRange) {
            const range = this.parseCustomRange();
            return (range.indexOf(value) / (range.length - 1)) * 100;
        }
        return ((value - this.min) / (this.max - this.min)) * 100;
    }
    getSlotContent(slotName) {
        const slot = this.host.querySelector(`[slot="${slotName}"]`);
        return slot === null || slot === void 0 ? void 0 : slot.innerHTML;
    }
    addFormsHandling() {
        if (this.name === undefined)
            return;
        const form = this.host.closest('form');
        if (form == null)
            return;
        form.addEventListener('formdata', (event) => {
            event.formData.append(this.name, this.value.toString());
        });
    }
    tooltip() {
        return this.host.shadowRoot.querySelector('.tooltip');
    }
    thumb() {
        return this.host.shadowRoot.querySelector('.thumb');
    }
    track() {
        return this.host.shadowRoot.querySelector('.track');
    }
    fill() {
        return this.host.shadowRoot.querySelector('.fill');
    }
    isLabelSlotProvided() {
        return !!this.host.querySelector('[slot="label"]');
    }
    isDescriptionSlotProvided() {
        return !!this.host.querySelector('[slot="description"]');
    }
    tickMarkCount() {
        if (this.step === 0)
            return 0;
        if (!this.showTickMarks && !this.marks)
            return 0;
        if (this.customRange)
            return this.parseCustomRange().length - 1;
        return (this.max - this.min) / this.step;
    }
    tickMarks() {
        const tickMarks = [];
        for (let i = 1; i < this.tickMarkCount(); i++) {
            const position = this.getTickPosition(i);
            const tickMark = (index.h("div", { class: "tick-mark", "data-value": i * this.step, style: { left: `${position.percentage}%` } }));
            tickMarks.push(tickMark);
        }
        return tickMarks;
    }
    markers() {
        const markers = [];
        for (let i = 0; i <= this.tickMarkCount(); i++) {
            const { nearestStep, percentage } = this.getTickPosition(i);
            markers.push(index.h("div", { class: "mark", "data-value": i * this.step, style: { left: `${percentage}%` } }));
            markers.push(index.h("div", { class: "label", "data-value": i * this.step, style: { left: `${percentage}%` } }, nearestStep, this.suffix));
        }
        return markers;
    }
    getTickPosition(i) {
        const range = this.parseCustomRange();
        let nearestStep;
        let percentage;
        if (this.customRange) {
            nearestStep = range[i];
            percentage = (i / (range.length - 1)) * 100;
        }
        else {
            nearestStep = this.roundToNearestStep(this.getMinimum() + this.step * i);
            percentage = this.percentageOnTrack(nearestStep);
        }
        return { nearestStep, percentage };
    }
    getAriaValueText() {
        let currentValue = this.value.toString();
        if (this.marks && this.suffix)
            currentValue += this.suffix;
        return currentValue;
    }
    isTopInfoAvailable() {
        return this.isLabelSlotProvided() || this.valueTemplate;
    }
    filledValueTemplate() {
        return this.valueTemplate.replace('{value}', this.value.toLocaleString(Slider.browserLanguage()));
    }
    static browserLanguage() {
        var _a;
        return (_a = window.navigator.language) === null || _a === void 0 ? void 0 : _a.slice(0, 2);
    }
    render() {
        return (index.h(index.Host, { key: 'b44775d83584e212b9593e5b34c6b9aedfddc0bf', role: "slider", "aria-valuemin": this.getMinimum(), "aria-valuemax": this.getMaximum(), "aria-valuetext": this.getAriaValueText(), tabIndex: this.disabled ? -1 : 0 }, this.isTopInfoAvailable() && (index.h("div", { key: '4dbe41548b50cef8c1d16293b80c949014f18405', class: "top-info", "aria-hidden": "true" }, this.isLabelSlotProvided() && (index.h("label", { key: 'af10cf94b8da66312bb47f107ba9095c0f4f9ceb', class: "label", id: "label" }, index.h("slot", { key: '892ac2a6a96788ed3a48690257c5f22f597f5b49', name: "label" }))), this.valueTemplate && index.h("div", { key: 'cb0e4c36e3b22e26cc32472030c0c813ae7c4ae9', class: "value" }, this.filledValueTemplate()))), index.h("div", { key: 'e6dba3e23d71b56d80b1da11fc9516e4ef227724', class: "track" }, index.h("div", { key: '36c1dc64e18ae4fab9c67ca59b3df007e9d193bf', class: "bar" }, this.showTickMarks && !this.marks && this.tickMarks(), index.h("div", { key: 'becf78a84781abe24351a9641a8973e0d3925fa0', class: "fill" }), index.h("div", { key: '8d3bac5bcce13a87d8ddbb044fe355339ceabbf9', class: "thumb", title: "slider" }), index.h("div", { key: 'be434d74a7e2ef4fa9179f62d6305816341c5ea4', class: "tooltip" }, index.h("span", { key: '54b9bf87556b756b6953f25e367ddd5baf4de00b' }, this.value.toString() + this.suffix)), this.marks && this.markers())), this.isDescriptionSlotProvided() && (index.h("div", { key: '99cfd03873f735456008c3b106a7e6109dc9c86d', class: { description: true, 'with-marks': this.marks }, "aria-hidden": "true" }, index.h("slot", { key: '9c97d121e750b1961479cfa46872983d4eae0440', name: "description" })))));
    }
    get host() { return index.getElement(this); }
    static get watchers() { return {
        "isDragged": [{
                "onDraggedChanged": 0
            }],
        "value": [{
                "onValueChanged": 0
            }]
    }; }
};
Slider.style = sliderCss();

exports.groupui_slider = Slider;
//# sourceMappingURL=groupui-slider.entry.cjs.js.map

//# sourceMappingURL=groupui-slider.cjs.entry.js.map