'use strict';

var index = require('./index-C35xP9d6.js');
var index$1 = require('./index-Dssn3hdS.js');
var a11y = require('./a11y-BUiqSzrG.js');
var a11yHelper = require('./a11y-helper-gj7bsNBC.js');

const selectCss = () => `:host{width:100%;font-family:var(--groupui-sys-font-family-primary), sans-serif;font-size:var(--groupui-sys-font-size-400);line-height:var(--groupui-sys-line-height-xxl);letter-spacing:var(--groupui-sys-letter-spacing-regular);font-weight:var(--groupui-sys-font-weight-normal);color:var(--groupui-sys-color-text-normal)}:host div{position:relative}:host button{all:unset;-webkit-box-sizing:border-box;box-sizing:border-box;color:var(--groupui-sys-color-action-basic-default)}:host #label,:host #description{font-size:var(--groupui-sys-font-size-350)}:host #label{display:block;cursor:auto;font-weight:var(--groupui-sys-font-weight-bold);margin-bottom:var(--groupui-sys-spacing-100)}:host #description{margin-top:var(--groupui-sys-spacing-100)}:host #placeholder{color:var(--groupui-sys-color-text-weakest);overflow:hidden;text-overflow:ellipsis}:host #icon{color:var(--groupui-sys-color-action-basic-default)}:host groupui-icon.status-icon{pointer-events:none}:host #select{cursor:pointer;display:-ms-flexbox;display:flex;width:100%;-ms-flex-pack:end;justify-content:flex-end;gap:var(--groupui-sys-spacing-200);-webkit-box-sizing:border-box;box-sizing:border-box;outline-offset:calc(-1 * var(--groupui-sys-border-width-s));border-radius:var(--groupui-sys-border-radius-s);padding:var(--groupui-sys-spacing-200) var(--groupui-sys-spacing-300);outline:var(--groupui-sys-border-width-s) var(--groupui-sys-stroke-style-primary) var(--groupui-sys-color-action-muted-default);background:var(--groupui-sys-color-action-silent-default)}:host #select:hover{outline-color:var(--groupui-sys-color-action-basic-default)}:host #select input{all:unset;-ms-flex-positive:1;flex-grow:1;min-width:0}:host #select #search-input{-ms-flex-preferred-size:0;flex-basis:0}:host #selected-option{min-height:calc(var(--groupui-sys-line-height-xxl) * 1em);white-space:nowrap;overflow:hidden;text-overflow:ellipsis}:host #selected-option .disabled{color:var(--groupui-sys-color-text-weakest)}:host #selected-option:not(:has(~input)){-ms-flex-positive:1;flex-grow:1}:host #selected-option:focus{outline:none}:host(:focus){outline:none}:host(:focus-within:not([disabled=""])) #clear.focused{outline:var(--groupui-sys-color-action-basic-default) var(--groupui-sys-stroke-style-primary) var(--groupui-sys-border-width-m);outline-offset:var(--groupui-sys-spacing-50)}:host(:focus-within:not([disabled=""])) #select:not(.clear-focus),:host(:focus-within:not([disabled=""])) #select.expanded{outline-width:var(--groupui-sys-border-width-m);outline-color:var(--groupui-sys-color-action-basic-default)}:host(:focus-within:not([disabled=""])) #select #search-input{-ms-flex-preferred-size:auto;flex-basis:auto}:host([size=s]) #select{padding-inline:var(--groupui-sys-spacing-200);height:calc(var(--groupui-sys-spacing-400) + var(--groupui-sys-spacing-200) * 2);-ms-flex-align:center;align-items:center}:host([size=s]) #selected-option{font-size:var(--groupui-sys-font-size-350)}:host([disabled=""]) #label,:host([disabled=""]) #description{color:var(--groupui-sys-color-action-basic-disabled)}:host([disabled=""]) #icon,:host([disabled=""]) #clear{color:var(--groupui-sys-color-action-basic-disabled)}:host([disabled=""]) #select{cursor:not-allowed;color:var(--groupui-sys-color-action-basic-disabled);background-color:var(--groupui-sys-color-action-silent-disabled);outline-color:var(--groupui-sys-color-action-basic-disabled)}:host([readonly=""]) #select{cursor:text;color:var(--groupui-sys-color-action-quiet-default);background-color:var(--groupui-sys-color-action-silent-disabled);border-color:var(--groupui-sys-color-action-basic-disabled)}:host([readonly=""]) #icon,:host([readonly=""]) #clear{color:var(--groupui-sys-color-action-basic-disabled)}:host([severity=success]:not([disabled=""])) #description{color:var(--groupui-sys-color-success-basic-default)}:host([severity=success]:not([disabled=""])) groupui-icon.status-icon{color:var(--groupui-sys-color-success-basic-default)}:host([severity=success]:not([disabled=""])) #select:not(.clear-focus){outline-color:var(--groupui-sys-color-success-basic-default)}:host([severity=warning]:not([disabled=""])) #description{color:var(--groupui-sys-color-warning-basic-default)}:host([severity=warning]:not([disabled=""])) groupui-icon.status-icon{color:var(--groupui-sys-color-warning-basic-default)}:host([severity=warning]:not([disabled=""])) #select:not(.clear-focus){outline-color:var(--groupui-sys-color-warning-basic-default)}:host([severity=danger]:not([disabled=""])) #description{color:var(--groupui-sys-color-danger-basic-default)}:host([severity=danger]:not([disabled=""])) groupui-icon.status-icon{color:var(--groupui-sys-color-danger-basic-default)}:host([severity=danger]:not([disabled=""])) #select:not(.clear-focus){outline-color:var(--groupui-sys-color-danger-basic-default)}`;

const Select = class {
    constructor(hostRef) {
        index.registerInstance(this, hostRef);
        this.groupuiChange = index.createEvent(this, "groupuiChange", 3);
        this.groupuiDidLoad = index.createEvent(this, "groupuiDidLoad", 3);
        this.groupuiInvalid = index.createEvent(this, "groupuiInvalid", 3);
        this.groupuiClearClicked = index.createEvent(this, "groupuiClearClicked", 3);
        this.groupuiSearchStarted = index.createEvent(this, "groupuiSearchStarted", 3);
        this.groupuiSearchCleared = index.createEvent(this, "groupuiSearchCleared", 3);
        this.groupuiInput = index.createEvent(this, "groupuiInput", 3);
        this.groupuiInputFocused = index.createEvent(this, "groupuiInputFocused", 3);
        /**
         * severity of the select
         */
        this.severity = 'none';
        /**
         * set max-height for the options container (e.g. "200px")
         */
        this.maxHeight = 'none';
        /**
         * specifies that an option must be selected before submitting the form
         */
        this.required = false;
        /**
         * show status icon on severity danger, warning, success
         */
        this.showStatusIcon = false;
        /**
         * shows the description only when the component has focus
         */
        this.descriptionOnlyOnFocus = false;
        /**
         * disable internal validation
         */
        this.noValidation = false;
        /**
         * toggle built-in search behaviour
         */
        this.builtInSearch = false;
        /**
         * makes the built-in search case-insensitive
         */
        this.caseInsensitiveSearch = false;
        /**
         * makes the select searchable
         */
        this.searchable = false;
        /**
         * sets the size of the input field
         */
        this.size = 'm';
        this.KEYBOARD_FOCUS = 'keyboard-focus';
        this.allOptions = [];
        this.allBaseOptions = [];
        this.focusableItems = [];
        this.inputTyping = new index$1.cjsExports.Subject();
        this.destroy = new index$1.cjsExports.Subject();
        this.keyboardFocus = undefined;
        this.handleOptionSlotChanged = async (event) => {
            const slot = event.target;
            this.assembleItemsList(slot.assignedElements());
            this.setOptionCallback();
        };
        this.handleClearIconClick = (e) => {
            if (this.searchOngoing) {
                this.clearSearchField();
            }
            else {
                this.value = undefined;
                e.stopPropagation();
                this.groupuiClearClicked.emit();
            }
            if (this.searchable) {
                this.getSearchInputElement().focus();
            }
            else {
                this.focusDropdown();
            }
        };
        this.handleOutsideClick = (event) => {
            if (!event.composedPath().includes(this.host)) {
                this.expanded = false;
            }
        };
        this.toggleExpanded = () => {
            this.expanded = !this.expanded;
        };
        this.setClearFocus = () => {
            if (this.tabbed) {
                this.clearFocus = true;
            }
            this.tabbed = false;
        };
        this.resetClearFocus = () => {
            this.clearFocus = false;
        };
        this.customAriaLabel = () => {
            if (this.a11yLabelClearButton)
                return this.a11yLabelClearButton;
            return a11y.CLEAR_BUTTON_LABEL;
        };
        this.onSearchInput = () => {
            const inputValue = this.getSearchInputElement().value;
            this.searchValue = inputValue;
            this.searchOngoing = inputValue !== '';
            this.expanded = true;
            this.groupuiInput.emit();
        };
        this.onSearchFocus = () => {
            this.groupuiInputFocused.emit();
        };
    }
    captureClick(event) {
        if (this.disabled || this.readonly) {
            event.stopPropagation();
        }
    }
    onKeyDown(event) {
        var _a;
        if (this.readonly || this.disabled) {
            return;
        }
        if (this.isSearchInputFocused()) {
            if (event.code === 'Space') {
                return;
            }
            if (event.code === 'Enter') {
                if (!((_a = this.keyboardFocus) === null || _a === void 0 ? void 0 : _a.type)) {
                    this.emitSearchStartedEvent();
                    return;
                }
            }
            if (event.code === 'Escape') {
                this.clearSearchField();
                return;
            }
        }
        if (event.code === 'Tab') {
            this.tabbed = true;
        }
        if (this.clearFocus) {
            if (event.code === 'Enter' || event.code === 'Space') {
                event.preventDefault();
                this.handleClearIconClick(event);
                if (this.searchable) {
                    this.getSearchInputElement().focus();
                }
                else {
                    this.focusDropdown();
                }
                return;
            }
        }
        if (this.expanded) {
            if (this.anySelectingKeyPressed(event)) {
                event.preventDefault();
                this.selectOption();
                if (this.searchable) {
                    this.getSearchInputElement().focus();
                }
                else {
                    this.focusDropdown();
                }
                return;
            }
            if (event.code === 'Tab') {
                this.selectOption();
                return;
            }
            if (event.code === 'ArrowDown') {
                event.preventDefault();
                this.focusOption('next');
                return;
            }
            if (event.code === 'ArrowUp') {
                event.preventDefault();
                this.focusOption('previous');
                return;
            }
        }
        if (event.code === 'Escape') {
            this.resetKeyboardFocus();
            this.expanded = false;
            if (!this.searchable) {
                this.focusDropdown();
            }
            return;
        }
        if (this.anyExpandingKeyPressed(event)) {
            event.preventDefault();
            this.expanded = true;
        }
    }
    onFocus() {
        if (this.disabled) {
            return;
        }
        if (this.searchable) {
            this.getSearchInputElement().focus();
        }
        this.hasFocus = true;
    }
    onBlur(event) {
        if (this.host.contains(event.relatedTarget))
            return;
        this.hasFocus = false;
        this.expanded = false;
        this.validateSelect();
    }
    onCheckboxGroupCollapseChanged(event) {
        this.updateFocusableItems();
        const index = this.focusableItems.findIndex((item) => item === event.target);
        this.setKeyboardFocus({
            index,
            element: event.target,
            type: 'option-group',
        });
    }
    onValueChange() {
        var _a;
        this.validateSelect();
        if (!this.valueMatchesOption() && !this.valueIsEmpty()) {
            console.warn(Select.CONSOLE_WARN_MESSAGE);
            return;
        }
        (_a = this.selectedIconObserver) === null || _a === void 0 ? void 0 : _a.disconnect();
        this.observeAllSelectedOptionIcons();
        this.host.dispatchEvent(new window.Event('change'));
        this.groupuiChange.emit();
    }
    onInvalidChange() {
        this.validity = this.getValidity();
        if (this.invalid) {
            this.severity = 'danger';
            this.host.dispatchEvent(new window.Event('invalid'));
            this.groupuiInvalid.emit();
        }
        else {
            this.severity = 'none';
        }
    }
    onExpandedChanged() {
        if (this.expanded) {
            document.addEventListener('mousedown', this.handleOutsideClick, false);
            if (this.searchable) {
                const searchInput = this.getSearchInputElement();
                searchInput.focus();
            }
        }
        else {
            document.removeEventListener('mousedown', this.handleOutsideClick, false);
            this.validateSelect();
        }
    }
    onSearchValueChange(newSearchValue) {
        this.inputTyping.next(newSearchValue);
    }
    /**
     * function to trigger component validation manually
     */
    async validate() {
        this.validateSelect({ force: true });
    }
    componentWillLoad() {
        this.ariaAttributes = a11yHelper.extractAriaAttributes(this.host);
        this.subscribeToInputChanges();
        this.validity = this.getValidity();
        if (this.getOptions().length > 0 && !this.valueMatchesOption() && !this.valueIsEmpty()) {
            console.warn(Select.CONSOLE_WARN_MESSAGE);
        }
        this.rerenderWhenIconsAreLoaded();
    }
    componentDidLoad() {
        this.autofocus && this.focusDropdown();
        this.addFormsHandling();
        this.removePreviousHighlights();
        this.groupuiDidLoad.emit();
    }
    componentDidRender() {
        if (this.anyOptionSelected()) {
            this.highlightSelectedOption();
        }
    }
    disconnectedCallback() {
        var _a;
        document.removeEventListener('mousedown', this.handleOutsideClick, false);
        (_a = this.selectedIconObserver) === null || _a === void 0 ? void 0 : _a.disconnect();
        this.destroy.next();
        this.destroy.complete();
    }
    subscribeToInputChanges() {
        const debouncedInputTyping = this.inputTyping.pipe(index$1.cjsExports.debounceTime(100));
        debouncedInputTyping.pipe(index$1.cjsExports.takeUntil(this.destroy)).subscribe((searchTerm) => {
            if (this.searchable && this.builtInSearch) {
                this.filterOptions(searchTerm);
                this.isEveryOptionFilteredOut = !this.isAnyOptionVisible();
            }
        });
    }
    iconSize() {
        return this.size === 's' ? 16 : 24;
    }
    isAnyOptionAvailable() {
        return this.getOptions().length > 0;
    }
    isAnyOptionVisible() {
        return Array.from(this.getOptions()).some((option) => {
            return !option.invisible;
        });
    }
    isPopoverVisible() {
        return this.expanded && !this.isEveryOptionFilteredOut && this.isAnyOptionAvailable();
    }
    getGroups() {
        return this.host.querySelectorAll('groupui-select-option-group');
    }
    filterOptions(searchTerm) {
        this.getOptions().forEach((option) => {
            if (this.caseInsensitiveSearch) {
                option.invisible = !option.textContent.toLowerCase().includes(searchTerm.toLowerCase());
            }
            else {
                option.invisible = !option.textContent.includes(searchTerm);
            }
        });
        this.getGroups().forEach((group) => {
            const groupOptions = group.querySelectorAll('groupui-select-option');
            group.invisible = Array.from(groupOptions).every((option) => option.invisible);
        });
        this.updateFocusableItems();
    }
    getOptions() {
        return this.host.querySelectorAll('groupui-select-option');
    }
    emitSearchStartedEvent() {
        const selectedOption = this.getSelectedOption();
        const selectedOptionEventPayload = selectedOption
            ? { value: selectedOption.value, content: selectedOption.textContent }
            : undefined;
        this.groupuiSearchStarted.emit({ selectedOption: selectedOptionEventPayload, searchString: this.searchValue });
    }
    isSearchInputFocused() {
        return this.searchable && this.getSearchInputElement() === this.host.shadowRoot.activeElement;
    }
    clearSearchField() {
        const searchInput = this.getSearchInputElement();
        if (!searchInput)
            return;
        searchInput.value = '';
        this.searchValue = '';
        this.searchOngoing = false;
        this.groupuiSearchCleared.emit();
    }
    assembleItemsList(assignedElements) {
        const allBaseOptions = [];
        const allOptions = [];
        assignedElements.forEach((element) => {
            const processElement = (element) => {
                if (element.tagName === 'GROUPUI-SELECT-OPTION') {
                    allBaseOptions.push({ type: 'option', element });
                    allOptions.push({ type: 'option', element });
                }
                else if (element.tagName === 'GROUPUI-SELECT-OPTION-GROUP') {
                    allBaseOptions.push({ type: 'option-group', element });
                    allOptions.push({ type: 'option-group', element });
                    element.childNodes.forEach((child) => {
                        if (child.tagName === 'GROUPUI-SELECT-OPTION') {
                            allOptions.push({ type: 'option', element: child });
                        }
                    });
                }
            };
            processElement(element);
        });
        this.allBaseOptions = allBaseOptions;
        this.allOptions = allOptions;
        this.updateFocusableItems();
    }
    updateFocusableItems() {
        const focusableItems = [];
        this.allBaseOptions.forEach((item) => {
            if (item.type === 'option' && (item.element.disabled || item.element.invisible))
                return;
            if (item.type === 'option') {
                focusableItems.push(item.element);
            }
            else if (item.type === 'option-group') {
                this.addOptionGroup(item, focusableItems);
                this.addOptionGroupChildren(item, focusableItems);
            }
        });
        this.focusableItems = focusableItems;
    }
    addOptionGroupChildren(optionGroup, focusableItems) {
        if (!optionGroup.element.collapsed) {
            const visibleChildren = Array.from(optionGroup.element.querySelectorAll('groupui-select-option')).filter((child) => !child.invisible);
            focusableItems.push(...visibleChildren);
        }
    }
    addOptionGroup(optionGroup, focusableItems) {
        if (!optionGroup.element.invisible && optionGroup.element.expandable) {
            focusableItems.push(optionGroup.element);
        }
    }
    getSearchInputElement() {
        return this.host.shadowRoot.querySelector('input[type="text"]');
    }
    getVisibleOptionEntries() {
        return this.focusableItems.map((option, index) => ({ index, option }));
    }
    rerenderWhenIconsAreLoaded() {
        this.selectedIconObserver = new MutationObserver(() => {
            index.forceUpdate(this);
        });
        this.observeAllSelectedOptionIcons();
    }
    observeAllSelectedOptionIcons() {
        var _a;
        const iconsInSelectedOption = (_a = this.getSelectedOption()) === null || _a === void 0 ? void 0 : _a.querySelectorAll('groupui-icon');
        iconsInSelectedOption && this.observeAllIcons(iconsInSelectedOption);
    }
    observeAllIcons(icons) {
        icons.forEach((icon) => {
            var _a;
            (_a = this.selectedIconObserver) === null || _a === void 0 ? void 0 : _a.observe(icon.shadowRoot, {
                subtree: true,
                childList: true,
            });
        });
    }
    getValidity() {
        return {
            valid: !this.required || !this.valueIsEmpty(),
            valueMissing: this.required && this.valueIsEmpty(),
            badInput: false,
            customError: false,
            patternMismatch: false,
            rangeOverflow: false,
            rangeUnderflow: false,
            stepMismatch: false,
            tooLong: false,
            tooShort: false,
            typeMismatch: false,
        };
    }
    anyExpandingKeyPressed(event) {
        return event.code === 'Enter' || event.code === 'ArrowDown' || event.code === 'ArrowUp' || event.code === 'Space';
    }
    anySelectingKeyPressed(event) {
        return event.code === 'Enter' || event.code === 'Space';
    }
    focusOption(direction) {
        const optionEntries = this.getVisibleOptionEntries();
        if (optionEntries.length === 0)
            return;
        const keyboardFocus = this.keyboardFocus;
        if (!keyboardFocus) {
            const initialIndex = direction === 'next' ? 0 : optionEntries.length - 1;
            this.setFocusToItem(optionEntries[initialIndex]);
            return;
        }
        const currentOptionIndex = keyboardFocus.index;
        const newEntryIndex = direction === 'next'
            ? (currentOptionIndex + 1) % optionEntries.length
            : (currentOptionIndex - 1 + optionEntries.length) % optionEntries.length;
        this.setFocusToItem(optionEntries[newEntryIndex]);
    }
    setFocusToItem(item) {
        if (item.option.tagName === 'GROUPUI-SELECT-OPTION') {
            this.setKeyboardFocus({
                type: 'option',
                index: item.index,
                element: item.option,
            });
        }
        else {
            this.setKeyboardFocus({
                type: 'option-group',
                index: item.index,
                element: item.option,
            });
        }
    }
    setKeyboardFocus(keyboardFocus) {
        if (this.keyboardFocus) {
            this.keyboardFocus.element.classList.remove(this.KEYBOARD_FOCUS);
        }
        this.keyboardFocus = keyboardFocus;
        this.keyboardFocus.element.classList.add(this.KEYBOARD_FOCUS);
    }
    resetKeyboardFocus() {
        if (this.keyboardFocus) {
            this.keyboardFocus.element.classList.remove(this.KEYBOARD_FOCUS);
            this.keyboardFocus = undefined;
        }
    }
    selectOption() {
        var _a, _b;
        if (((_a = this.keyboardFocus) === null || _a === void 0 ? void 0 : _a.type) === 'option-group') {
            this.keyboardFocus.element.collapsed = !this.keyboardFocus.element.collapsed;
        }
        else if (((_b = this.keyboardFocus) === null || _b === void 0 ? void 0 : _b.type) === 'option') {
            this.onOptionSelected(this.keyboardFocus.element.value)();
        }
    }
    valueIsEmpty() {
        return this.value === undefined || this.value === null || this.isEmptyString();
    }
    isEmptyString() {
        return this.value.trim && this.value.trim() === '';
    }
    getSelectedOption() {
        return Array.from(this.host.querySelectorAll('groupui-select-option')).find((element) => element.value === this.value);
    }
    getSelectedOptionGroup() {
        const matchingOptionGroup = this.allBaseOptions
            .filter((option) => option.type === 'option-group')
            .find((optionGroup) => {
            const allOptionsOfOptionGroup = Array.from(optionGroup.element.querySelectorAll('groupui-select-option'));
            return allOptionsOfOptionGroup.some((option) => option.value === this.value);
        });
        return matchingOptionGroup === null || matchingOptionGroup === void 0 ? void 0 : matchingOptionGroup.element;
    }
    validateSelect({ force = false } = {}) {
        if (!force && this.noValidation)
            return;
        this.invalid = !this.getValidity().valid;
    }
    isClearIconVisible() {
        return this.searchOngoing || (this.showClearIcon && this.anyOptionSelected());
    }
    addFormsHandling() {
        if (this.name === undefined)
            return;
        const form = this.host.closest('form');
        if (form == null)
            return;
        form.addEventListener('formdata', (event) => {
            event.formData.append(this.name, this.value);
        });
        form.addEventListener('submit', (event) => {
            this.validateSelect();
            if (this.invalid) {
                event.preventDefault();
            }
        });
    }
    highlightSelectedOption() {
        var _a, _b;
        (_a = this.getSelectedOption()) === null || _a === void 0 ? void 0 : _a.classList.add('selected');
        (_b = this.getSelectedOptionGroup()) === null || _b === void 0 ? void 0 : _b.classList.add('selected');
    }
    focusDropdown() {
        this.host.focus &&
            this.host.shadowRoot.querySelector('#selected-option').focus();
    }
    setOptionCallback() {
        Array.from(this.host.querySelectorAll('groupui-select-option')).forEach((option) => {
            option.addEventListener('groupuiOptionSelected', this.onOptionSelected(option.value));
        });
    }
    onOptionSelected(value) {
        return () => {
            this.removePreviousHighlights();
            this.value = value;
            this.resetKeyboardFocus();
            this.expanded = false;
            this.focusDropdown();
            this.clearSearchField();
        };
    }
    removePreviousHighlights() {
        this.allOptions.forEach((option) => {
            option.element.classList.remove('selected');
        });
    }
    isLabelSlotProvided() {
        return !!this.host.querySelector('[slot="label"]');
    }
    isDescriptionSlotProvided() {
        return !!this.host.querySelector('[slot="description"]');
    }
    placeholderVisible() {
        return this.placeholder && !this.anyOptionSelected();
    }
    anyOptionSelected() {
        return !this.valueIsEmpty() && this.valueMatchesOption();
    }
    getInnerHTMLOfSelectedOption() {
        const selectedOption = this.getSelectedOption();
        if (!selectedOption)
            return '';
        const clone = selectedOption.cloneNode(true);
        const description = clone.querySelector('[slot="description"]');
        if (description) {
            description.remove();
        }
        return clone.innerHTML;
    }
    valueMatchesOption() {
        return Array.from(this.host.querySelectorAll('groupui-select-option')).some((element) => element.value === this.value);
    }
    isDisabledOption() {
        const selectedOption = this.getSelectedOption();
        return selectedOption.disabled;
    }
    useStatusIcon() {
        return this.showStatusIcon && this.severity && this.severity !== 'none';
    }
    getStatusIcon() {
        switch (this.severity) {
            case 'warning':
                return `warning-filled-${this.iconSize()}`;
            case 'danger':
                return `error-filled-${this.iconSize()}`;
            case 'success':
                return `success-filled-${this.iconSize()}`;
        }
    }
    showDescription() {
        if (!this.descriptionOnlyOnFocus) {
            return true;
        }
        else {
            return this.hasFocus;
        }
    }
    render() {
        return (index.h(index.Host, { key: '3692631b68fd5f848a3d7bd2b8155fc5757b4769' }, this.isLabelSlotProvided() && (index.h("label", { key: 'b6a6f32d277e67b14f66e712edd1c566c97464b0', id: "label", onClick: this.toggleExpanded, "aria-hidden": "true" }, index.h("slot", { key: '647f92a174f32879f96530188a8f77cb107f413f', name: "label" }))), index.h("div", { key: '38c066fb7b7593c3a13cf557182878860db6ebb5', id: "input-wrapper" }, index.h("groupui-popover", { key: '0d93b55b91c535578898f4e48468a5019e8ae92b', visible: this.isPopoverVisible(), "full-width": true }, index.h("div", { key: '04cb2f2daf0ef0da8eea34bb510d1802cd01f204', slot: "parent", id: "select", onClick: this.toggleExpanded, class: { 'clear-focus': this.clearFocus ? true : undefined } }, index.h("span", { key: 'b367912112caae0087029ecebed4473dff8096dd', id: "selected-option", tabIndex: this.disabled || this.expanded || this.searchable ? -1 : 0, role: "combobox", "aria-labelledby": this.isLabelSlotProvided() ? 'label' : undefined, "aria-describedby": this.isDescriptionSlotProvided() ? 'description' : undefined, "aria-expanded": this.expanded ? 'true' : 'false', "aria-controls": "content", class: {
                expanded: this.expanded ? true : undefined,
                focused: this.hasFocus ? true : undefined,
            }, ...this.ariaAttributes }, this.placeholderVisible() && index.h("div", { key: '350f04c096259528756e5f0734c4cbf37913967c', id: "placeholder" }, this.placeholder), this.anyOptionSelected() && (index.h("span", { key: 'c40e2d939a3408f2ca3b47a36bcc2ac4983eb5ad', class: this.isDisabledOption() ? 'disabled' : '', innerHTML: this.getInnerHTMLOfSelectedOption() }))), this.searchable && (index.h("input", { key: '55e825cd0b3639a55277e7542281f2f1a581f088', type: "text", id: "search-input", onInput: this.onSearchInput, onFocus: this.onSearchFocus, value: this.searchValue })), this.useStatusIcon() && (index.h("groupui-icon", { key: 'c270bcbea681a781508cfe17d8184c7dfadb281c', name: this.getStatusIcon(), width: this.iconSize(), height: this.iconSize(), class: "status-icon", "aria-hidden": "true" })), this.isClearIconVisible() && (index.h("groupui-embedded-button", { key: '1733549572e839f7b245bde3c9d99829f169d448', id: "clear", icon: `close-${this.iconSize()}`, size: this.size === 'm' ? 'xs' : 'xxs', onClick: this.handleClearIconClick, onBlur: this.resetClearFocus, onFocus: this.setClearFocus, "aria-label": this.customAriaLabel(), "aria-describedby": this.isLabelSlotProvided() && !this.a11yLabelClearButton ? 'label' : undefined, "aria-hidden": this.readonly || this.disabled ? 'true' : null, disabled: this.readonly || this.disabled })), index.h("groupui-icon", { key: '1c83f8fd9f11c6ede48922f7f62dc59b35aed51b', id: "icon", name: this.expanded ? `chevron-up-${this.iconSize()}` : `chevron-down-${this.iconSize()}`, "aria-hidden": "true" })), index.h("div", { key: '56800d5e5983ba58e058d0615dc2d105f367ad2d', slot: "content", id: "content", style: { maxHeight: this.maxHeight } }, index.h("slot", { key: 'c2261d269699b71278d3c2ba20e572b226df4c49', onSlotchange: this.handleOptionSlotChanged })))), this.isDescriptionSlotProvided() && this.showDescription() && (index.h("div", { key: 'd67d278bbf8bfa6c5d27234491b607587b9dc650', id: "description", onClick: this.toggleExpanded, "aria-hidden": "true" }, index.h("slot", { key: '61dd61fe959dd6d0d78262c735ba7684b231abd5', name: "description" })))));
    }
    static get delegatesFocus() { return true; }
    get host() { return index.getElement(this); }
    static get watchers() { return {
        "value": [{
                "onValueChange": 0
            }],
        "invalid": [{
                "onInvalidChange": 0
            }],
        "expanded": [{
                "onExpandedChanged": 0
            }],
        "searchValue": [{
                "onSearchValueChange": 0
            }]
    }; }
};
Select.CONSOLE_WARN_MESSAGE = 'Predefined value not found in any groupui-select-option';
Select.style = selectCss();

const selectOptionCss = () => `:host{display:block;width:100%;outline:none;font-family:var(--groupui-sys-font-family-primary), sans-serif;font-size:var(--groupui-sys-font-size-400);line-height:var(--groupui-sys-line-height-xxl);letter-spacing:var(--groupui-sys-letter-spacing-regular)}:host #container{display:-ms-flexbox;display:flex;-ms-flex-direction:column;flex-direction:column;cursor:pointer;-webkit-box-sizing:border-box;box-sizing:border-box;width:100%;padding:var(--groupui-sys-spacing-200) var(--groupui-sys-spacing-300);margin:var(--groupui-sys-spacing-0);color:var(--groupui-sys-color-navigation-unselected-basic-default);background-color:var(--groupui-sys-color-background-base)}:host #container:hover{background-color:var(--groupui-sys-color-action-subtle-hover);color:var(--groupui-sys-color-navigation-selected-basic-default);-webkit-transition:all var(--groupui-sys-duration-fast-1) var(--groupui-sys-cubic-bezier-ease-in-out);transition:all var(--groupui-sys-duration-fast-1) var(--groupui-sys-cubic-bezier-ease-in-out)}:host #container ::slotted([slot=description]){color:var(--groupui-sys-color-text-weaker)}:host(.selected) #container{color:var(--groupui-sys-color-navigation-selected-basic-default)}:host(.selected) #container ::slotted([slot=description]){color:var(--groupui-sys-color-text-weak)}:host(.keyboard-focus){outline:0}:host(.keyboard-focus) #container{background-color:var(--groupui-sys-color-action-subtle-hover);color:var(--groupui-sys-color-navigation-selected-basic-default)}:host([disabled=""]) #container,:host([disabled=""]) #container:hover,:host([disabled=""]) #container:active{cursor:not-allowed;color:var(--groupui-sys-color-text-weakest);background-color:var(--groupui-sys-color-background-base)}:host([disabled=""]) #container ::slotted([slot=description]),:host([disabled=""]) #container:hover ::slotted([slot=description]),:host([disabled=""]) #container:active ::slotted([slot=description]){color:var(--groupui-sys-color-text-weakest)}:host(.group-indentation) #container{padding-left:var(--groupui-sys-spacing-800)}:host([invisible=""]){display:none}`;

const SelectOption = class {
    constructor(hostRef) {
        index.registerInstance(this, hostRef);
        this.groupuiDidLoad = index.createEvent(this, "groupuiDidLoad", 3);
        this.groupuiOptionSelected = index.createEvent(this, "groupuiOptionSelected", 3);
        /**
         * value of the select-option to identify which option is selected.
         */
        this.value = Math.random().toString(36).substring(7);
        /**
         * marks this option as disabled, so it is shown in the select but is not selectable.
         */
        this.disabled = false;
        /**
         * use this only with a searchable select, hides the option
         */
        this.invisible = false;
    }
    componentDidLoad() {
        this.groupuiDidLoad.emit();
    }
    captureClick(event) {
        event.stopPropagation();
        if (!this.disabled) {
            this.groupuiOptionSelected.emit(this.value);
        }
    }
    isDescriptionSlotProvided() {
        return !!this.host.querySelector('[slot="description"]');
    }
    render() {
        return (index.h(index.Host, { key: 'e1daa4a0deccffea76306aa481050f87ab5b2380', tabindex: -1, role: "option" }, index.h("div", { key: 'e875a8af6c927636d69d238a2c78137d63fa0700', id: "container" }, index.h("slot", { key: '062607c41774ed0a72480ce4099ff2245ae38965' }), this.isDescriptionSlotProvided() && index.h("slot", { key: 'f9703644bd9a2c1ce9c7edacaa172c1c561ff236', name: "description" }))));
    }
    get host() { return index.getElement(this); }
};
SelectOption.style = selectOptionCss();

const simplePaginationCss = () => `:host{letter-spacing:var(--groupui-sys-letter-spacing-regular);font-family:var(--groupui-sys-font-family-primary), sans-serif;font-size:var(--groupui-sys-font-size-350);font-weight:var(--groupui-sys-font-weight-action);color:var(--groupui-sys-color-action-basic-default)}:host nav{display:-ms-inline-flexbox;display:inline-flex;gap:var(--groupui-sys-spacing-100)}:host button{all:unset;-webkit-box-sizing:border-box;box-sizing:border-box}:host .page,:host .placeholder{text-align:center;line-height:var(--groupui-sys-spacing-800);border-radius:var(--groupui-sys-border-radius-xl)}:host .placeholder{color:var(--groupui-sys-color-action-basic-disabled)}:host .page{padding-inline:var(--groupui-sys-spacing-300);cursor:pointer;-webkit-transition:background-color var(--groupui-sys-duration-fast-1) var(--groupui-sys-cubic-bezier-ease-in-out);transition:background-color var(--groupui-sys-duration-fast-1) var(--groupui-sys-cubic-bezier-ease-in-out)}:host .page:hover{background-color:var(--groupui-sys-color-action-subtle-hover)}:host .page:active{background-color:var(--groupui-sys-color-action-subtle-pressed)}:host .page.selected{font-weight:var(--groupui-sys-font-weight-action);color:var(--groupui-sys-color-on-action-basic-default);background-color:var(--groupui-sys-color-action-basic-default)}:host .page:focus-visible{outline-offset:var(--groupui-sys-spacing-50);outline:var(--groupui-sys-color-action-basic-default) var(--groupui-sys-stroke-style-primary) var(--groupui-sys-border-width-m)}:host span{width:var(--groupui-sys-spacing-800);height:var(--groupui-sys-spacing-800)}:host(:focus-visible){outline:none}`;

const SimplePagination = class {
    constructor(hostRef) {
        index.registerInstance(this, hostRef);
        this.groupuiPageChange = index.createEvent(this, "groupuiPageChange", 1);
        this.groupuiDidLoad = index.createEvent(this, "groupuiDidLoad", 3);
        /**
         * Number of displayed pages. Minimum is five. Values below five have no effect.
         */
        this.displayedPageCount = 5;
        /**
         * Selected page
         */
        this.selectedPage = 1;
        /**
         * aria-label for the previous button. The default value is 'Previous'.
         */
        this.a11yLabelPreviousButton = a11y.PREVIOUS_BUTTON_LABEL;
        /**
         * aria-label for the next button. The default value is 'Next'.
         */
        this.a11yLabelNextButton = a11y.NEXT_BUTTON_LABEL;
        this.selectPage = (page) => {
            this.selectedPage = page;
        };
        this.selectPreviousPage = () => {
            this.selectedPage = Math.max(1, this.selectedPage - 1);
        };
        this.selectNextPage = () => {
            this.selectedPage = Math.min(this.pageCount, this.selectedPage + 1);
        };
    }
    onSelectedPageChanged() {
        this.groupuiPageChange.emit(this.selectedPage);
    }
    onKeyDown(event) {
        if (this.isAnySelectingKeyPressed(event) && this.isPageFocused()) {
            const focusedPage = this.host.shadowRoot.activeElement.getAttribute('data-page');
            this.selectedPage = parseInt(focusedPage);
            event.preventDefault();
        }
    }
    componentDidLoad() {
        this.groupuiDidLoad.emit();
    }
    isPageFocused() {
        return this.host.shadowRoot.activeElement.hasAttribute('data-page');
    }
    isAnySelectingKeyPressed(event) {
        return event.code === 'Enter' || event.code === 'Space';
    }
    getAriaLabel() {
        const paginationParentElement = this.host.closest('[aria-label="pagination"]');
        return paginationParentElement ? undefined : 'pagination';
    }
    pageElements() {
        const pages = Array.from({ length: this.pageCount }, (_, i) => i + 1);
        const sanitizedDisplayedPageCount = this.displayedPageCount >= 5 ? this.displayedPageCount : 5;
        let result = [];
        if (this.pageCount > sanitizedDisplayedPageCount) {
            const staticPages = this.staticPages(pages);
            result = staticPages;
            const missingPages = pages.filter((page) => !staticPages.includes(page));
            let displayedPagesLeftCount = sanitizedDisplayedPageCount - staticPages.length;
            if (displayedPagesLeftCount > 0) {
                if (this.selectedPage === undefined) {
                    result = result.concat(missingPages.slice(0, displayedPagesLeftCount));
                }
                else {
                    while (displayedPagesLeftCount > 0) {
                        const nearestPage = this.findNearestPage(missingPages, this.selectedPage);
                        result.push(nearestPage);
                        displayedPagesLeftCount--;
                        this.removePage(missingPages, nearestPage);
                    }
                }
            }
            result = result.sort((a, b) => a - b);
        }
        else {
            result = pages;
        }
        return result.flatMap((page, index$1) => {
            const elements = [];
            if (this.isGap(index$1, page, result)) {
                elements.push(index.h("div", { class: "placeholder" }, "..."));
            }
            elements.push(index.h("button", { tabIndex: 0, "data-page": page, class: page === this.selectedPage ? 'page selected' : 'page', "aria-current": page === this.selectedPage ? 'page' : undefined, onClick: () => this.selectPage(page) }, index.h("span", null, page)));
            return elements;
        });
    }
    isGap(index, page, pages) {
        return index > 0 && page - 1 !== pages[index - 1];
    }
    staticPages(pages) {
        return pages.filter((page) => this.isFirstPage(page) ||
            this.isLastPage(page) ||
            this.isPreviousPage(page) ||
            this.isNextPage(page) ||
            this.isSelectedPage(page));
    }
    removePage(missingPages, nearestPage) {
        const nearestPageIndex = missingPages.indexOf(nearestPage);
        missingPages.splice(nearestPageIndex, 1);
    }
    findNearestPage(pages, page) {
        return pages.reduce((prev, curr) => (Math.abs(curr - page) <= Math.abs(prev - page) ? curr : prev));
    }
    isFirstPage(pageNumber) {
        return pageNumber === 1;
    }
    isLastPage(pageNumber) {
        return pageNumber === this.pageCount;
    }
    isPreviousPage(pageNumber) {
        return this.isPreviousPageAvailable() && pageNumber === this.selectedPage - 1;
    }
    isNextPage(pageNumber) {
        return this.isNextPageAvailable() && pageNumber === this.selectedPage + 1;
    }
    isPreviousPageAvailable() {
        return this.selectedPage !== undefined && this.selectedPage > 1;
    }
    isNextPageAvailable() {
        return this.selectedPage !== undefined && this.selectedPage < this.pageCount;
    }
    isSelectedPage(pageNumber) {
        return pageNumber === this.selectedPage;
    }
    isPreviousButtonDisabled() {
        return this.selectedPage === 1;
    }
    isNextButtonDisabled() {
        return this.selectedPage >= this.pageCount;
    }
    isPreviousButtonIconOnly() {
        return this.previousButtonText === undefined;
    }
    isNextButtonIconOnly() {
        return this.nextButtonText === undefined;
    }
    render() {
        return (index.h(index.Host, { key: 'dcc5b9fdcde79b28b889d85013d6d3af48fafced' }, index.h("nav", { key: 'cdf28c083edf07629295e2954be538ca36261534', "aria-label": this.getAriaLabel() }, index.h("groupui-button", { key: '561fe3425793c1df3e6db29f92a0410a5b6b0712', id: "pagination-button-previous", tabIndex: 0, class: "pagination__previous", iconLeft: "chevron-left-16", variant: "tertiary", size: "s", "aria-label": this.a11yLabelPreviousButton, "icon-only": this.isPreviousButtonIconOnly(), onClick: this.selectPreviousPage, disabled: this.isPreviousButtonDisabled() }, this.previousButtonText), this.pageElements(), index.h("groupui-button", { key: '2b881fd241cbffb5c0b7e9b0dcfda676bedc4267', id: "pagination-button-next", tabIndex: 0, class: "pagination__next", "icon-right": "chevron-right-16", variant: "tertiary", size: "s", "aria-label": this.a11yLabelNextButton, "icon-only": this.isNextButtonIconOnly(), onClick: this.selectNextPage, disabled: this.isNextButtonDisabled() }, this.nextButtonText))));
    }
    get host() { return index.getElement(this); }
    static get watchers() { return {
        "selectedPage": [{
                "onSelectedPageChanged": 0
            }]
    }; }
};
SimplePagination.style = simplePaginationCss();

exports.groupui_select = Select;
exports.groupui_select_option = SelectOption;
exports.groupui_simple_pagination = SimplePagination;
//# sourceMappingURL=groupui-select.groupui-select-option.groupui-simple-pagination.entry.cjs.js.map

//# sourceMappingURL=groupui-select_3.cjs.entry.js.map