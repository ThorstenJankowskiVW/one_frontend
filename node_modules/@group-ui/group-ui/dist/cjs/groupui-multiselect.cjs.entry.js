'use strict';

var index = require('./index-C35xP9d6.js');
var index$1 = require('./index-Dssn3hdS.js');
var a11y = require('./a11y-BUiqSzrG.js');
var a11yHelper = require('./a11y-helper-gj7bsNBC.js');

const createSelectAllOption = (element, selectAll, deselectAll) => {
    const clickListener = () => {
        if (element.indeterminate)
            selectAll();
    };
    const checkedChangedListener = () => {
        if (element.checked)
            selectAll();
        else
            deselectAll();
    };
    const setState = (state) => {
        if (state === 'checked') {
            element.indeterminate = false;
            element.checked = true;
        }
        else if (state === 'indeterminate') {
            element.indeterminate = true;
        }
        else {
            element.checked = false;
            element.indeterminate = false;
        }
    };
    const addListener = () => {
        element.addEventListener('click', clickListener);
        element.addEventListener('checkedChanged', checkedChangedListener);
    };
    const removeListener = () => {
        element.removeEventListener('click', clickListener);
        element.removeEventListener('checkedChanged', checkedChangedListener);
    };
    return {
        element,
        setState,
        addListener,
        removeListener,
    };
};

const multiselectCss = () => `:host #container{position:relative;cursor:pointer;display:-ms-flexbox;display:flex;-ms-flex-pack:start;justify-content:start;outline:none;resize:none;-webkit-box-sizing:border-box;box-sizing:border-box;width:100%;overflow-y:auto;min-height:var(--groupui-sys-spacing-1000);margin:var(--groupui-sys-spacing-0);padding:calc(var(--groupui-sys-spacing-200) - var(--groupui-sys-border-width-s)) calc(var(--groupui-sys-spacing-300) - var(--groupui-sys-border-width-s));color:var(--groupui-sys-color-text-normal);background-color:var(--groupui-sys-color-action-silent-default);font-family:var(--groupui-sys-font-family-primary), sans-serif;font-size:var(--groupui-sys-font-size-400);line-height:var(--groupui-sys-line-height-xxl);letter-spacing:var(--groupui-sys-letter-spacing-regular);border-radius:var(--groupui-sys-border-radius-s);border:var(--groupui-sys-border-width-s) var(--groupui-sys-stroke-style-primary) var(--groupui-sys-color-action-muted-default)}:host #container input{display:block;position:relative;color:inherit;background-color:inherit;resize:none;width:var(--groupui-sys-spacing-1200);margin:var(--groupui-sys-spacing-0);padding:var(--groupui-sys-spacing-0)}:host #container input:not(.input-focus){caret-color:transparent}:host #container input,:host #container #input-width{-webkit-box-sizing:border-box;box-sizing:border-box;border:none;outline:none;font-family:var(--groupui-sys-font-family-primary), sans-serif;font-size:var(--groupui-sys-font-size-400);line-height:var(--groupui-sys-line-height-xxl);letter-spacing:var(--groupui-sys-letter-spacing-regular)}:host #container #input-width-wrapper{position:relative;left:auto}:host #container:not(.expanded) #input-width-wrapper{position:absolute;left:-9999px;top:-9999px}:host #container #input-width{position:absolute;padding:var(--groupui-sys-spacing-0);min-width:var(--groupui-sys-spacing-1200);left:-9999px;top:-9999px}:host #container #placeholder{text-overflow:ellipsis;white-space:nowrap;overflow:hidden;color:var(--groupui-sys-color-text-weakest)}:host #container groupui-chip groupui-icon{position:static}:host #container #selected-options-wrapper{display:-ms-flexbox;display:flex;white-space:pre-wrap;-ms-flex-wrap:wrap;flex-wrap:wrap;gap:var(--groupui-sys-spacing-200);width:100%;min-width:0}:host #container #selected-options-wrapper:has(#selected-options #placeholder){gap:0}:host #container #selected-options{display:contents}:host #container:hover{border-color:var(--groupui-sys-color-action-basic-default)}:host #container.expanded:not(.sub-focus){border-color:var(--groupui-sys-color-action-basic-default);-webkit-box-shadow:0 0 0 var(--groupui-sys-border-width-s) var(--groupui-sys-color-action-basic-default);box-shadow:0 0 0 var(--groupui-sys-border-width-s) var(--groupui-sys-color-action-basic-default)}:host #container.expanded:not(.sub-focus) #chevron{-webkit-transform:rotate(180deg);transform:rotate(180deg)}:host #container.expanded:not(.sub-focus) #input-width-wrapper{position:relative;left:auto}:host([size=s]) #container{padding-inline:var(--groupui-sys-spacing-200);min-height:calc(var(--groupui-sys-line-height-s) * 1rem);font-size:var(--groupui-sys-font-size-350);line-height:var(--groupui-sys-line-height-s)}:host([size=s]) #container input{font-size:var(--groupui-sys-font-size-350);line-height:var(--groupui-sys-line-height-s)}:host(:focus-within:not([disabled=""])) #container:not(.sub-focus){border-color:var(--groupui-sys-color-action-basic-default);-webkit-box-shadow:0 0 0 var(--groupui-sys-border-width-s) var(--groupui-sys-color-action-basic-default);box-shadow:0 0 0 var(--groupui-sys-border-width-s) var(--groupui-sys-color-action-basic-default)}:host([single-line=""]:not([show-chips=""])) #container #selected-options-wrapper{white-space:nowrap;overflow:hidden}:host([single-line=""]:not([show-chips=""])) #container #selected-options{display:initial;text-overflow:ellipsis}:host([searchable=""][show-chips=""]) #container #selected-options-wrapper{cursor:text}:host([disabled=""]) #container groupui-icon,:host([readonly=""]) #container groupui-icon{cursor:not-allowed;color:var(--groupui-sys-color-action-basic-disabled)}:host([disabled=""]) #container{cursor:not-allowed;background-color:var(--groupui-sys-color-action-silent-disabled);border-color:var(--groupui-sys-color-action-basic-disabled);color:var(--groupui-sys-color-action-basic-disabled)}:host([readonly=""]) #container{cursor:text;color:var(--groupui-sys-color-text-normal);background-color:var(--groupui-sys-color-action-silent-disabled)}:host([severity=success]:not([disabled=""])) #description,:host([severity=success]:not([disabled=""])) ::slotted([slot=description]),:host([severity=success]:not([disabled=""])) #status-icon{color:var(--groupui-sys-color-success-basic-default)}:host([severity=success]:not([disabled=""])) #container{border-color:var(--groupui-sys-color-success-basic-default)}:host([severity=success]:not([disabled=""])) #container.expanded{border-color:var(--groupui-sys-color-success-basic-default);-webkit-box-shadow:0 0 0 var(--groupui-sys-border-width-s) var(--groupui-sys-color-success-basic-default);box-shadow:0 0 0 var(--groupui-sys-border-width-s) var(--groupui-sys-color-success-basic-default)}:host(:focus-within[severity=success]:not([disabled=""])) #container{-webkit-box-shadow:0 0 0 var(--groupui-sys-border-width-s) var(--groupui-sys-color-success-basic-default);box-shadow:0 0 0 var(--groupui-sys-border-width-s) var(--groupui-sys-color-success-basic-default)}:host([severity=warning]:not([disabled=""])) #description,:host([severity=warning]:not([disabled=""])) ::slotted([slot=description]),:host([severity=warning]:not([disabled=""])) #status-icon{color:var(--groupui-sys-color-warning-basic-default)}:host([severity=warning]:not([disabled=""])) #container{border-color:var(--groupui-sys-color-warning-basic-default)}:host([severity=warning]:not([disabled=""])) #container.expanded{border-color:var(--groupui-sys-color-warning-basic-default);-webkit-box-shadow:0 0 0 var(--groupui-sys-border-width-s) var(--groupui-sys-color-warning-basic-default);box-shadow:0 0 0 var(--groupui-sys-border-width-s) var(--groupui-sys-color-warning-basic-default)}:host(:focus-within[severity=warning]:not([disabled=""])) #container{-webkit-box-shadow:0 0 0 var(--groupui-sys-border-width-s) var(--groupui-sys-color-warning-basic-default);box-shadow:0 0 0 var(--groupui-sys-border-width-s) var(--groupui-sys-color-warning-basic-default)}:host([severity=danger]:not([disabled=""])) #description,:host([severity=danger]:not([disabled=""])) ::slotted([slot=description]),:host([severity=danger]:not([disabled=""])) #status-icon{color:var(--groupui-sys-color-danger-basic-default)}:host([severity=danger]:not([disabled=""])) #container{border-color:var(--groupui-sys-color-danger-basic-default)}:host([severity=danger]:not([disabled=""])) #container.expanded{border-color:var(--groupui-sys-color-danger-basic-default);-webkit-box-shadow:0 0 0 var(--groupui-sys-border-width-s) var(--groupui-sys-color-danger-basic-default);box-shadow:0 0 0 var(--groupui-sys-border-width-s) var(--groupui-sys-color-danger-basic-default)}:host(:focus-within[severity=danger]:not([disabled=""])) #container{-webkit-box-shadow:0 0 0 var(--groupui-sys-border-width-s) var(--groupui-sys-color-danger-basic-default);box-shadow:0 0 0 var(--groupui-sys-border-width-s) var(--groupui-sys-color-danger-basic-default)}:host{width:100%;font-family:var(--groupui-sys-font-family-primary), sans-serif;font-size:var(--groupui-sys-font-size-400);line-height:var(--groupui-sys-line-height-xxl);letter-spacing:var(--groupui-sys-letter-spacing-regular)}:host button{all:unset;-webkit-box-sizing:border-box;box-sizing:border-box}:host #label,:host #description{display:block;cursor:default;font-size:var(--groupui-sys-font-size-350)}:host #label{margin-bottom:var(--groupui-sys-spacing-100)}:host ::slotted([slot=label]){color:var(--groupui-sys-color-text-normal);font-weight:var(--groupui-sys-font-weight-bold)}:host #description{margin-top:var(--groupui-sys-spacing-100)}:host ::slotted([slot=description]){color:var(--groupui-sys-color-text-normal)}:host #select{width:100%}:host #icons{display:-ms-flexbox;display:flex;position:-webkit-sticky;position:sticky;top:0;-ms-flex-negative:0;flex-shrink:0;-ms-flex-align:center;align-items:center;gap:var(--groupui-sys-spacing-200);padding-left:var(--groupui-sys-spacing-200)}:host #icons groupui-icon{color:var(--groupui-sys-color-action-basic-default)}:host #outer-chips-container{display:-ms-flexbox;display:flex;-ms-flex-wrap:wrap;flex-wrap:wrap;margin-top:var(--groupui-sys-spacing-200);gap:var(--groupui-sys-spacing-200)}:host(:focus){outline:0}:host(:focus) #input-wrapper,:host(:focus) #select{outline:0}:host([disabled=""]){pointer-events:auto}:host([disabled=""]) #label,:host([disabled=""]) #description,:host([disabled=""]) ::slotted([slot=label]),:host([disabled=""]) ::slotted([slot=description]){color:var(--groupui-sys-color-action-basic-disabled)}:host([disabled=""]) #placeholder{color:var(--groupui-sys-color-action-basic-disabled)}groupui-popover:focus-visible{outline:none}`;

const Multiselect = class {
    constructor(hostRef) {
        index.registerInstance(this, hostRef);
        this.groupuiChange = index.createEvent(this, "groupuiChange", 3);
        this.groupuiDidLoad = index.createEvent(this, "groupuiDidLoad", 3);
        this.groupuiClearClicked = index.createEvent(this, "groupuiClearClicked", 3);
        this.groupuiSearchStarted = index.createEvent(this, "groupuiSearchStarted", 3);
        this.groupuiSearchCleared = index.createEvent(this, "groupuiSearchCleared", 3);
        this.groupuiInput = index.createEvent(this, "groupuiInput", 3);
        this.groupuiInputFocused = index.createEvent(this, "groupuiInputFocused", 3);
        this.optionsCountToken = '{count}';
        this.selectedOptionsToken = '{options}';
        this.allOptionsCountToken = '{countAll}';
        /**
         * severity of the multiselect component
         */
        this.severity = 'none';
        /**
         * sets the size of the input field
         */
        this.size = 'm';
        /**
         * set max-height for the options container (e.g. "200px")
         */
        this.maxHeight = 'none';
        /**
         * set max-height for the input container (e.g. "200px")
         */
        this.inputMaxHeight = 'none';
        /**
         * specifies that at least one option must be selected before submitting the form
         */
        this.required = false;
        /**
         * show status icon on severity danger, warning, success
         */
        this.showStatusIcon = false;
        /**
         * shows the description only when the component has focus
         */
        this.descriptionOnlyOnFocus = false;
        /**
         * disable internal validation
         */
        this.noValidation = false;
        /**
         * shows chips for selected options<br>
         * should not be used with size="s"
         */
        this.showChips = false;
        /**
         * position the chips below the multiselect
         */
        this.positionChipsBelow = false;
        /**
         * filters options only when show chips is activated
         */
        this.searchable = false;
        /**
         * shows selected options only on one line, does not work with chips
         */
        this.singleLine = false;
        /**
         * toggle built-in search behaviour
         */
        this.builtInSearch = false;
        /**
         * makes the built-in search case-insensitive
         */
        this.caseInsensitiveSearch = false;
        this.keyboardFocus = { type: 'none' };
        this.selectedMultiselectOptions = [];
        this.KEYBOARD_FOCUS = 'keyboard-focus';
        this.inputTyping = new index$1.cjsExports.Subject();
        this.outsideClick = new index$1.cjsExports.Observable();
        this.destroy = new index$1.cjsExports.Subject();
        this.allItems = [];
        this.selectableOptions = [];
        this.focusableItems = [];
        this.ignoreCheckedChangedEvents = false;
        this.handleOutsideClick = (event) => {
            if (this.expanded && !event.composedPath().includes(this.host)) {
                this.hideOptions();
                if (this.searchable) {
                    this.clearSearchField();
                    this.triggerAutoGrowing();
                    this.searchOngoing = false;
                    this.setKeyboardFocus({ type: 'none' });
                }
            }
        };
        this.handleClearIconClick = (e) => {
            var _a, _b;
            e.stopPropagation();
            if (!this.searchOngoing) {
                (_a = this.selectAllOption) === null || _a === void 0 ? void 0 : _a.removeListener();
                this.changeAllOptions(false);
                (_b = this.selectAllOption) === null || _b === void 0 ? void 0 : _b.addListener();
                this.groupuiClearClicked.emit();
            }
            else {
                this.clearInput();
                this.searchValue = '';
                this.groupuiSearchCleared.emit();
            }
        };
        this.toggleExpanded = (event) => {
            const element = event.target;
            const targetChip = element.closest('groupui-chip');
            if (!(targetChip && targetChip.nodeName === 'GROUPUI-CHIP') && !this.searchOngoing) {
                this.expanded = !this.expanded;
                this.focusSelectedOptions();
            }
        };
        this.handleOptionSlotChanged = async (event) => {
            const slot = event.target;
            this.assembleItemsList(slot.assignedElements());
        };
        this.handleInput = () => {
            const inputValue = this.getInputElement().value;
            this.searchOngoing = inputValue !== '';
            this.searchValue = inputValue;
            this.showOptions();
            this.groupuiInput.emit();
        };
        this.clearInput = () => {
            const input = this.getInputElement();
            input.value = '';
            this.searchValue = '';
            this.searchable && this.triggerAutoGrowing();
            this.searchOngoing = false;
            input.focus();
        };
        this.setInputFocus = () => {
            this.setKeyboardFocus({ type: 'input' });
            this.groupuiInputFocused.emit();
        };
        this.setSubFocus = (event) => {
            if (this.tabbed) {
                this.setKeyboardFocus({ type: event.target.id });
            }
        };
    }
    subscribeToInputChanges() {
        const debouncedInputTyping = this.inputTyping.pipe(index$1.cjsExports.debounceTime(100));
        debouncedInputTyping.pipe(index$1.cjsExports.takeUntil(this.destroy)).subscribe((searchTerm) => {
            if (this.searchable && this.builtInSearch) {
                this.filterOptions(searchTerm);
                this.isEveryOptionFilteredOut = !this.isAnyOptionVisible();
            }
        });
    }
    onSearchValueChange(newSearchValue) {
        this.inputTyping.next(newSearchValue);
    }
    filterOptions(searchTerm) {
        this.getOptions().forEach((option) => {
            if (this.caseInsensitiveSearch) {
                option.invisible = !option.textContent.toLowerCase().includes(searchTerm.toLowerCase());
            }
            else {
                option.invisible = !option.textContent.includes(searchTerm);
            }
        });
        this.getGroups().forEach((group) => {
            const groupOptions = group.querySelectorAll('groupui-multiselect-option');
            group.invisible = Array.from(groupOptions).every((option) => option.invisible);
        });
        this.getGroups().forEach((group) => {
            const isChildOptionsMatchingSearchTerm = Array.from(group.querySelectorAll('groupui-multiselect-option')).some((option) => !option.invisible);
            if (isChildOptionsMatchingSearchTerm) {
                group.collapsed = false;
            }
        });
        this.updateFocusableItems();
    }
    getOptions() {
        return this.host.querySelectorAll('groupui-multiselect-option');
    }
    getGroups() {
        return this.host.querySelectorAll('groupui-multiselect-option-group');
    }
    iconSize() {
        return this.size === 's' ? 16 : 24;
    }
    captureClick(event) {
        if (this.disabled || this.readonly) {
            event.stopPropagation();
        }
        this.searchable && !this.positionChipsBelow && this.getInputElement().focus();
    }
    async onKeyDown(event) {
        var _a, _b;
        if (this.readonly) {
            return;
        }
        // focus last chip on ArrowLeft if input is focused and search input is empty
        if (this.keyboardFocus.type === 'input') {
            if (!this.searchOngoing && event.code === 'ArrowLeft') {
                this.focusPreviousChip();
                return;
            }
            if (event.code === 'Enter') {
                this.emitSearchStartedEvent();
                return;
            }
        }
        // skip keyboard handling during user search input
        if (this.keyboardFocus.type === 'input' && !['Escape', 'Tab', 'ArrowUp', 'ArrowDown'].includes(event.code))
            return;
        if (event.code !== 'Tab') {
            event.preventDefault();
        }
        switch (event.code) {
            case 'Tab':
                this.tabbed = true;
                break;
            case 'Enter':
            case 'Space':
                this.showOptions();
                this.toggleActiveOption();
                break;
            case 'Escape':
                this.hideOptions();
                this.setKeyboardFocus(this.searchable ? { type: 'input' } : { type: 'none' });
                this.searchOngoing = false;
                this.clearSearchField();
                break;
            case 'ArrowLeft':
                if (((_a = this.keyboardFocus) === null || _a === void 0 ? void 0 : _a.type) === 'group') {
                    this.keyboardFocus.element.collapsed = true;
                }
                else
                    this.focusPreviousChip();
                break;
            case 'ArrowRight':
                if (((_b = this.keyboardFocus) === null || _b === void 0 ? void 0 : _b.type) === 'group') {
                    this.keyboardFocus.element.collapsed = false;
                }
                else
                    this.focusNextChip();
                break;
            case 'ArrowDown':
                this.focusOption('next');
                break;
            case 'ArrowUp':
                this.focusOption('previous');
                break;
            case 'Backspace':
                this.clickActiveChip();
                break;
        }
    }
    clearSearchField() {
        var _a;
        const inputElement = this.getInputElement();
        if (!inputElement)
            return;
        if (((_a = inputElement.value) === null || _a === void 0 ? void 0 : _a.length) > 0) {
            inputElement.value = '';
            this.searchValue = '';
            this.groupuiSearchCleared.emit();
        }
    }
    emitSearchStartedEvent() {
        this.groupuiSearchStarted.emit({ selectedOptions: this.getEventSelectedOptions(), searchString: this.searchValue });
    }
    onOptionGroupCollapseChanged() {
        this.updateFocusableItems();
    }
    onParentCheckboxChanged(event) {
        const options = event.target.querySelectorAll('groupui-multiselect-option');
        this.changeMultipleOptions(Array.from(options), event.detail);
    }
    async onCheckedChanged(event) {
        var _a;
        if (event.target === ((_a = this.selectAllOption) === null || _a === void 0 ? void 0 : _a.element) || this.ignoreCheckedChangedEvents)
            return;
        this.updateItems();
    }
    updateItems() {
        this.handleOptionChange();
        this.focusSelectedOptions();
        this.validateMultiselect();
    }
    onFocus() {
        var _a;
        this.hasFocus = true;
        if (this.searchable)
            (_a = this.getInputElement()) === null || _a === void 0 ? void 0 : _a.focus();
    }
    onBlur(event) {
        this.hasFocus = false;
        this.setKeyboardFocus({ type: 'none' });
        if (this.tabbed) {
            this.tabbed = false;
            if (!this.newFocusInChild(event)) {
                this.hideOptions();
                if (this.searchable) {
                    this.clearSearchField();
                }
                this.searchOngoing = false;
            }
        }
    }
    newFocusInChild(event) {
        const currentTarget = event.currentTarget;
        const relatedTarget = event.relatedTarget;
        return event.relatedTarget && currentTarget.contains(relatedTarget);
    }
    onExpandedChanged() {
        if (!this.expanded) {
            this.validateMultiselect();
            this.searchOngoing = false;
        }
    }
    onInvalidChange() {
        this.validity = this.getValidity();
        this.invalid ? (this.severity = 'danger') : (this.severity = null);
    }
    /**
     * function to trigger component validation manually
     */
    async validate() {
        this.validateMultiselect({ force: true });
    }
    componentWillLoad() {
        this.ariaAttributes = a11yHelper.extractAriaAttributes(this.host);
        this.subscribeToInputChanges();
        this.subscribeToOutsideClicks();
    }
    subscribeToOutsideClicks() {
        this.outsideClick = index$1.cjsExports.fromEvent(document, 'click').pipe(index$1.cjsExports.takeUntil(this.destroy));
        this.outsideClick.subscribe((event) => {
            this.handleOutsideClick(event);
        });
    }
    isAnyOptionVisible() {
        return Array.from(this.getOptions()).some((option) => {
            return !option.invisible;
        });
    }
    isAnyOptionAvailable() {
        return this.getOptions().length > 0;
    }
    async componentDidLoad() {
        await this.waitForParentsOnReady();
        this.thereCanBeOnlyOneSelectAll();
        this.autofocus && this.host.focus && this.host.focus();
        this.addFormsHandling();
        this.addAutoGrowingInput();
        this.validity = this.getValidity();
        this.groupuiDidLoad.emit();
    }
    thereCanBeOnlyOneSelectAll() {
        if (this.host.querySelectorAll('groupui-multiselect-option[select-all=""]').length > 1) {
            console.warn('Only one multiselect-option with select-all per multiselect allowed');
        }
    }
    addAutoGrowingInput() {
        if (this.searchable) {
            this.getInputElement().addEventListener('input', () => this.triggerAutoGrowing());
        }
    }
    triggerAutoGrowing() {
        const input = this.getInputElement();
        const inputWidth = this.host.shadowRoot.querySelector('#input-width');
        const selectedOptions = this.host.shadowRoot.querySelector('#selected-options-wrapper');
        // In Safari the caret takes up space inside the input
        const caretWidth = 2;
        inputWidth.innerHTML = input.value.replace(/\s/g, '&nbsp;');
        if (selectedOptions.offsetWidth > inputWidth.offsetWidth) {
            input.style.width = inputWidth.offsetWidth + caretWidth + 'px';
        }
    }
    setKeyboardFocus(keyboardFocus) {
        this.removeKeyboardFocus();
        this.keyboardFocus = keyboardFocus;
        this.applyKeyboardFocus();
    }
    validateMultiselect({ force = false } = {}) {
        if (!force && this.noValidation)
            return;
        this.invalid = !this.getValidity().valid;
    }
    getValidity() {
        return {
            valid: this.anyOptionSelected() || !this.required,
            valueMissing: this.required && !this.anyOptionSelected(),
            badInput: false,
            customError: false,
            patternMismatch: false,
            rangeOverflow: false,
            rangeUnderflow: false,
            stepMismatch: false,
            tooLong: false,
            tooShort: false,
            typeMismatch: false,
        };
    }
    async waitForParentsOnReady() {
        const multiselect = document.querySelector('groupui-multiselect');
        if (multiselect) {
            let parent = multiselect.parentElement;
            while (parent.componentOnReady) {
                await parent.componentOnReady();
                parent = parent.parentElement;
            }
        }
    }
    disconnectedCallback() {
        this.destroy.next();
        this.destroy.complete();
    }
    showOptions() {
        this.expanded = true;
    }
    hideOptions() {
        this.expanded = false;
    }
    removeKeyboardFocus() {
        if (this.keyboardFocus.type === 'option' ||
            this.keyboardFocus.type === 'group' ||
            this.keyboardFocus.type === 'chip') {
            this.keyboardFocus.element.classList.remove(this.KEYBOARD_FOCUS);
        }
    }
    applyKeyboardFocus() {
        if (this.keyboardFocus.type === 'none')
            return;
        switch (this.keyboardFocus.type) {
            case 'input':
                this.searchable && this.getInputElement().focus();
                break;
            case 'chip':
            case 'group':
            case 'option':
                this.keyboardFocus.element.scrollIntoView({ block: 'nearest' });
                this.keyboardFocus.element.classList.add(this.KEYBOARD_FOCUS);
                if (this.keyboardFocus.type !== 'chip')
                    this.showOptions();
                break;
        }
    }
    getVisibleOptionEntries() {
        return this.focusableItems.map((option, index) => ({ index, option }));
    }
    focusOption(direction) {
        const optionEntries = this.getVisibleOptionEntries();
        if (optionEntries.length === 0)
            return;
        const keyboardFocus = this.keyboardFocus;
        if (!keyboardFocus || (keyboardFocus.type !== 'option' && keyboardFocus.type !== 'group')) {
            const initialIndex = direction === 'next' ? 0 : optionEntries.length - 1;
            this.setFocusToItem(optionEntries[initialIndex]);
            return;
        }
        const currentOptionIndex = keyboardFocus === null || keyboardFocus === void 0 ? void 0 : keyboardFocus.index;
        if (this.searchable &&
            ((direction === 'next' && currentOptionIndex + 1 >= optionEntries.length) ||
                (direction === 'previous' && currentOptionIndex - 1 < 0))) {
            this.setKeyboardFocus({ type: 'input' });
            return;
        }
        const newEntryIndex = direction === 'next'
            ? (currentOptionIndex + 1) % optionEntries.length
            : (currentOptionIndex - 1 + optionEntries.length) % optionEntries.length;
        this.setFocusToItem(optionEntries[newEntryIndex]);
    }
    setFocusToItem(item) {
        if (item.option.tagName === 'GROUPUI-MULTISELECT-OPTION') {
            this.setKeyboardFocus({
                type: 'option',
                index: item.index,
                element: item.option,
            });
        }
        else {
            this.setKeyboardFocus({
                type: 'group',
                index: item.index,
                element: item.option,
            });
        }
    }
    toggleActiveOption() {
        var _a;
        if (((_a = this.keyboardFocus) === null || _a === void 0 ? void 0 : _a.type) === 'group') {
            this.keyboardFocus.element.click();
        }
        else if (this.keyboardFocus.type === 'option') {
            this.keyboardFocus.element.checked = !this.keyboardFocus.element.checked;
        }
    }
    getChips() {
        return Array.from(this.host.shadowRoot.querySelectorAll('groupui-chip'));
    }
    focusNextChip() {
        const chips = this.getChips();
        if (chips.length === 0)
            return;
        if (this.keyboardFocus.type !== 'chip') {
            this.setKeyboardFocus({ type: 'chip', index: 0, element: chips[0] });
        }
        else if (this.searchable && this.keyboardFocus.index + 1 >= chips.length) {
            this.setKeyboardFocus({ type: 'input' });
        }
        else {
            const index = Math.min(chips.length - 1, this.keyboardFocus.index + 1);
            this.setKeyboardFocus({
                type: 'chip',
                index,
                element: chips[index],
            });
        }
    }
    focusPreviousChip() {
        const chips = this.getChips();
        if (chips.length === 0)
            return;
        if (this.keyboardFocus.type !== 'chip') {
            const index = chips.length - 1;
            this.setKeyboardFocus({ type: 'chip', index, element: chips[index] });
        }
        else {
            const index = Math.max(0, this.keyboardFocus.index - 1);
            this.setKeyboardFocus({ type: 'chip', index, element: chips[index] });
        }
    }
    focusNextChipAfterDelete() {
        if (this.keyboardFocus.type !== 'chip')
            return;
        const chips = this.getChips();
        if (chips.length > 0) {
            const index = Math.max(0, this.keyboardFocus.index - 1);
            this.setKeyboardFocus({
                type: 'chip',
                index,
                element: chips[index],
            });
        }
        else if (this.searchable) {
            this.setKeyboardFocus({ type: 'input' });
        }
        else {
            this.setKeyboardFocus({ type: 'none' });
        }
    }
    clickActiveChip() {
        var _a;
        if (this.keyboardFocus.type === 'chip') {
            (_a = this.getChips()[this.keyboardFocus.index]) === null || _a === void 0 ? void 0 : _a.click();
        }
    }
    addFormsHandling() {
        if (this.name === undefined)
            return;
        const form = this.host.closest('form');
        if (form == null)
            return;
        form.addEventListener('formdata', (event) => {
            this.getSelectedOptions().forEach((option) => {
                event.formData.append(this.name, option.value);
            });
        });
        form.addEventListener('submit', (event) => {
            this.validateMultiselect();
            if (this.invalid) {
                event.preventDefault();
            }
        });
    }
    handleOptionChange() {
        this.updateSelectedMultiselectOptions();
        this.dispatchEvents();
    }
    updateSelectedMultiselectOptions() {
        const selectedOptions = this.selectableOptions.filter((option) => option.checked);
        const selectedMultiselectOptions = [];
        this.allItems.forEach((item) => {
            if (item.type === 'option' && selectedOptions.includes(item.element)) {
                selectedMultiselectOptions.push({ options: [item.element], label: this.filterOptionLabel(item.element) });
            }
            else if (item.type === 'expandable-option-group') {
                const optionGroupOptions = Array.from(item.element.querySelectorAll('groupui-multiselect-option'));
                if (optionGroupOptions.every((option) => option.checked)) {
                    selectedMultiselectOptions.push({
                        label: item.element.label,
                        options: optionGroupOptions,
                    });
                }
                else {
                    selectedMultiselectOptions.push(...optionGroupOptions
                        .filter((option) => option.checked)
                        .map((option) => {
                        return { options: [option], label: this.filterOptionLabel(option) };
                    }));
                }
            }
        });
        this.selectedMultiselectOptions = selectedMultiselectOptions;
        this.updateSelectAllState();
    }
    filterOptionLabel(option) {
        var _a;
        let htmlContent = (_a = option.innerHTML) === null || _a === void 0 ? void 0 : _a.trim();
        let start, end;
        if (option.querySelector('[slot="description"]')) {
            start = htmlContent.indexOf(option.querySelector('[slot="description"]').outerHTML);
            end = option.querySelector('[slot="description"]').outerHTML.length;
            if (start === 0) {
                htmlContent = htmlContent.substring(end);
            }
            else {
                htmlContent = htmlContent.substring(0, start);
            }
        }
        return htmlContent.trim();
    }
    dispatchEvents() {
        const eventSelectedOptions = this.getEventSelectedOptions();
        this.host.dispatchEvent(new CustomEvent('change', { detail: { selectedOptions: eventSelectedOptions, searchString: this.searchValue } }));
        this.groupuiChange.emit({ selectedOptions: eventSelectedOptions, searchString: this.searchValue });
    }
    getSelectedOptions() {
        return this.selectedMultiselectOptions.flatMap((option) => option.options);
    }
    updateSelectAllState() {
        var _a;
        const selectedOptionsCount = this.getSelectedOptions().length;
        (_a = this.selectAllOption) === null || _a === void 0 ? void 0 : _a.setState(selectedOptionsCount === 0
            ? 'unchecked'
            : selectedOptionsCount === this.selectableOptions.length
                ? 'checked'
                : 'indeterminate');
    }
    changeMultipleOptions(options, checked) {
        this.ignoreCheckedChangedEvents = true;
        options.forEach((option) => {
            option.checked = checked;
        });
        this.ignoreCheckedChangedEvents = false;
        this.updateItems();
    }
    changeAllOptions(checkedState) {
        this.changeMultipleOptions(this.selectableOptions, checkedState);
    }
    getEventSelectedOptions() {
        return this.getSelectedOptions().map((option) => {
            return { value: option.value, content: option.innerText };
        });
    }
    removeOptionFromSelection(option) {
        this.changeMultipleOptions(option.options, false);
        this.focusNextChipAfterDelete();
    }
    renderSelectedOptions() {
        if (this.summary && !this.isPlaceholderVisible()) {
            return this.summary
                .replace(this.optionsCountToken, `${this.getSelectedOptions().length}`)
                .replace(this.allOptionsCountToken, `${this.selectableOptions.length}`)
                .replace(this.selectedOptionsToken, this.joinSelectedOptions());
        }
        else {
            return this.joinSelectedOptions();
        }
    }
    isPlaceholderVisible() {
        if (!this.placeholder || this.placeholder === '')
            return false;
        if (this.searchable && this.hasFocus)
            return false;
        return this.getSelectedOptions().length === 0 || this.positionChipsBelow;
    }
    anyOptionSelected() {
        return Array.from(this.selectableOptions).some((option) => option.checked);
    }
    focusSelectedOptions() {
        const selectedOptions = this.host.shadowRoot.querySelector('#selected-options');
        selectedOptions && selectedOptions.focus && selectedOptions.focus();
    }
    joinSelectedOptions() {
        return this.selectedMultiselectOptions.map((selectedOption) => selectedOption.label).join(', ');
    }
    createChips() {
        return this.selectedMultiselectOptions.map((option) => (index.h("groupui-chip", { embedded: true, disabled: this.disabled, color: "light", onClick: () => this.removeOptionFromSelection(option) }, index.h("span", { innerHTML: option.label }), !this.disabled && (index.h("div", { slot: "end" }, index.h("groupui-icon", { name: "close-16" }))))));
    }
    isLabelSlotProvided() {
        return !!this.host.querySelector('[slot="label"]');
    }
    isDescriptionSlotProvided() {
        return Array.from(this.host.children).some((option) => {
            return option.getAttribute('slot') === 'description';
        });
    }
    useStatusIcon() {
        return this.showStatusIcon && this.severity && this.severity !== 'none';
    }
    getStatusIcon() {
        switch (this.severity) {
            case 'warning':
                return `warning-filled-${this.iconSize()}`;
            case 'danger':
                return `error-filled-${this.iconSize()}`;
            case 'success':
                return `success-filled-${this.iconSize()}`;
        }
    }
    showDescription() {
        return !this.descriptionOnlyOnFocus ? true : this.hasFocus;
    }
    updateFocusableItems() {
        const focusableItems = [];
        this.allItems.forEach((item) => {
            if (item.element.invisible)
                return;
            if (item.type === 'option') {
                focusableItems.push(item.element);
            }
            else if (item.type === 'expandable-option-group') {
                focusableItems.push(item.element);
                if (!item.element.collapsed) {
                    const options = Array.from(item.element.querySelectorAll('groupui-multiselect-option'));
                    options.forEach((child) => {
                        if (!child.invisible) {
                            focusableItems.push(child);
                        }
                    });
                }
            }
        });
        this.focusableItems = focusableItems;
    }
    assembleItemsList(assignedElements) {
        const allItems = [];
        const selectableOptions = [];
        assignedElements.forEach((element) => {
            const processElement = (element) => {
                if (element.tagName === 'GROUPUI-MULTISELECT-OPTION') {
                    allItems.push({ type: 'option', element });
                    selectableOptions.push(element);
                }
                else if (element.tagName === 'GROUPUI-MULTISELECT-OPTION-GROUP' && element.hasAttribute('expandable')) {
                    allItems.push({ type: 'expandable-option-group', element });
                    element.childNodes.forEach((child) => {
                        if (child.tagName === 'GROUPUI-MULTISELECT-OPTION') {
                            selectableOptions.push(child);
                        }
                    });
                }
                else if (element.tagName === 'GROUPUI-MULTISELECT-OPTION-GROUP') {
                    element.childNodes.forEach((child) => {
                        processElement(child);
                    });
                }
            };
            processElement(element);
        });
        this.allItems = allItems;
        this.setupSelectAllOption(selectableOptions);
        this.selectableOptions = selectableOptions.filter((option) => !option.selectAll);
        this.updateFocusableItems();
        this.handleOptionChange();
        this.updateSelectAllState();
    }
    setupSelectAllOption(selectableOptions) {
        var _a;
        (_a = this.selectAllOption) === null || _a === void 0 ? void 0 : _a.removeListener();
        const selectAll = selectableOptions.find((option) => option.selectAll);
        if (selectAll) {
            this.selectAllOption = createSelectAllOption(selectAll, () => {
                this.changeAllOptions(true);
            }, () => {
                this.changeAllOptions(false);
            });
            this.selectAllOption.addListener();
        }
    }
    getInputElement() {
        return this.host.shadowRoot.querySelector('input');
    }
    isClearIconVisible() {
        return this.searchOngoing || (this.showClearIcon && this.anyOptionSelected());
    }
    isPopoverVisible() {
        return this.expanded && !this.isEveryOptionFilteredOut && this.isAnyOptionAvailable();
    }
    render() {
        return (index.h(index.Host, { key: '545f3adcd6189680614780a3c9a3f4004f128421' }, this.isLabelSlotProvided() && (index.h("label", { key: '41841ba9af17db3a7fb78aa1d514b5d60fa27651', id: "label", onClick: this.toggleExpanded, "aria-hidden": "true" }, index.h("slot", { key: '86feb88507a933b528f44a4618ba3f3b4589b37c', name: "label" }))), index.h("div", { key: 'af7371561f39a1299a562c716a79876150ddbd67', id: "input-wrapper", tabIndex: this.disabled || this.searchable ? -1 : 0 }, index.h("groupui-popover", { key: 'f8d98f14d3b13913cf0ee8339db34a939671a714', visible: this.isPopoverVisible(), "full-width": true, "max-height": this.maxHeight, offset: undefined }, index.h("div", { key: '20ae99c158527e9803a97eb2e0fa57333305c5e5', slot: "parent", role: "combobox", id: "select", onClick: this.toggleExpanded, class: this.expanded ? 'expanded' : undefined, "aria-labelledby": this.isLabelSlotProvided() ? 'label' : undefined, "aria-describedby": this.isDescriptionSlotProvided() ? 'description' : undefined, "aria-expanded": this.expanded ? 'true' : 'false', "aria-controls": "content", ...this.ariaAttributes }, index.h("div", { key: '4e0c52cb38da339cb4083cfd22f94c20cb40012b', id: "container", style: { maxHeight: this.inputMaxHeight }, class: {
                expanded: this.expanded,
                filled: this.getSelectedOptions().length > 0,
                statusicon: this.useStatusIcon(),
                'sub-focus': this.keyboardFocus.type === 'clear' || this.keyboardFocus.type === 'search',
            } }, index.h("div", { key: 'aaa64444a08955a61069a5ce6dfb5ee4a31896a5', id: "selected-options-wrapper" }, index.h("div", { key: '0fb54c7fad5bd10e694e55bab846fe34865ff454', id: "selected-options" }, this.isPlaceholderVisible() && index.h("div", { key: '84a3226698d903d7467809568b58216dd700cd8c', id: "placeholder" }, this.placeholder), this.showChips && !this.positionChipsBelow && this.createChips(), !this.showChips && index.h("span", { key: 'cfc6c5fb9eba623e6c2425d1b6f2262fe8a57c44', innerHTML: this.renderSelectedOptions() })), this.searchable && (index.h(index.Fragment, { key: '7ebb1697776cbbc4b0a9d4a44f8c16d3e453ceee' }, index.h("div", { key: '6d7c6d9ba5b4ab11d60b87c46edb7118a1e2c2ef', id: "input-width-wrapper" }, index.h("input", { key: '1773fba0aa8be9c72cada582ec75ff2f94a3d235', class: this.keyboardFocus.type === 'input' ? 'input-focus' : '', onInput: this.handleInput, onFocus: this.setInputFocus, tabindex: 0, disabled: this.disabled, readonly: this.readonly, value: this.searchValue, "aria-labelledby": this.isLabelSlotProvided() ? 'label' : undefined, "aria-describedby": this.isDescriptionSlotProvided() ? 'description' : undefined, ...this.ariaAttributes }), index.h("span", { key: '916e72193a0df350a2ac4af94de383bcc0c5ba20', id: "input-width", "aria-hidden": "true" }))))), index.h("div", { key: '26f818f69600b28463eea60f894295d40b3f3e80', id: "icons" }, this.useStatusIcon() && (index.h("groupui-icon", { key: 'f264927b5d8b481645be257db6a24b9868a1d430', id: "status-icon", name: this.getStatusIcon(), width: this.iconSize(), height: this.iconSize(), "aria-hidden": "true" })), this.isClearIconVisible() && (index.h("groupui-embedded-button", { key: '02ab4aac7788f0a0e0b3a4f9be4775671b45e3a0', id: "clear", onClick: this.handleClearIconClick, onFocus: this.setSubFocus, class: this.keyboardFocus.type === 'clear' ? 'focused' : '', "aria-label": this.a11yLabelClearButton ? this.a11yLabelClearButton : a11y.CLEAR_SEARCH_BUTTON_LABEL, "aria-describedby": this.isLabelSlotProvided() && !this.a11yLabelClearButton ? 'label' : null, "aria-hidden": this.disabled || this.readonly ? 'true' : null, disabled: this.disabled || this.readonly, icon: `close-${this.iconSize()}`, size: this.size === 'm' ? 'xs' : 'xxs' })), index.h("groupui-icon", { key: 'f61832f1195f2d622bd4c4e4e3ffc836bfbeb624', id: "chevron", name: `chevron-down-${this.iconSize()}`, "aria-hidden": "true" })))), index.h("div", { key: 'de01fa272ef129fc21a39c58e6f03577b49c324c', slot: "content", id: "content", style: { maxHeight: this.maxHeight } }, index.h("slot", { key: 'c2b0516f7bf8de988fcd0265785b01b4abd3c40a', onSlotchange: this.handleOptionSlotChanged })))), this.isDescriptionSlotProvided() && this.showDescription() && (index.h("div", { key: '407575254b72dbb601ce362020ef01dfcc2265dd', id: "description", onClick: this.toggleExpanded, "aria-hidden": "true" }, index.h("slot", { key: '3f4b5bb9737fc57d254cfb0bb5e1c114ef374817', name: "description" }))), this.showChips && this.positionChipsBelow && (index.h("div", { key: '0fa7186f86e1540a85d315a2a67fcbdbbb31ce4d', id: "outer-chips-container", onMouseDown: (e) => {
                e.preventDefault();
            } }, this.createChips()))));
    }
    static get delegatesFocus() { return true; }
    get host() { return index.getElement(this); }
    static get watchers() { return {
        "searchValue": [{
                "onSearchValueChange": 0
            }],
        "expanded": [{
                "onExpandedChanged": 0
            }],
        "invalid": [{
                "onInvalidChange": 0
            }]
    }; }
};
Multiselect.style = multiselectCss();

exports.groupui_multiselect = Multiselect;
//# sourceMappingURL=groupui-multiselect.entry.cjs.js.map

//# sourceMappingURL=groupui-multiselect.cjs.entry.js.map