const SUPPORTED_ARIA_ATTRIBUTES = [
    'aria-label',
    'aria-description',
    'aria-expanded',
    'role',
    'aria-describedby',
];
function extractAriaAttributes(hostElement) {
    const attributes = Array.from(hostElement.attributes).filter((attr) => SUPPORTED_ARIA_ATTRIBUTES.includes(attr.name));
    attributes.forEach((attr) => hostElement.removeAttribute(attr.name));
    return {
        ...attributes.reduce((acc, attr) => ({ ...acc, [attr.name]: attr.value }), {}),
    };
}

export { extractAriaAttributes as e };
//# sourceMappingURL=a11y-helper-C9YUnJbQ.js.map

//# sourceMappingURL=a11y-helper-C9YUnJbQ.js.map