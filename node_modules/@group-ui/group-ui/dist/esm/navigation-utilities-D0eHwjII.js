class NavigationUtilities {
    isLeafItem(item, selectors) {
        return item.querySelectorAll(selectors).length === 0;
    }
    hasActiveChildItems(item, selectors) {
        const childItems = item.querySelectorAll(selectors);
        return Array.from(childItems).some((child) => {
            return child.hasAttribute('active');
        });
    }
    getAllItems(element, selectors) {
        return Array.from(element.querySelectorAll(selectors));
    }
    removeActiveFromAllItems(element, selectors) {
        this.getAllItems(element, selectors).forEach((el) => el.removeAttribute('active'));
    }
    setActiveItem(item) {
        item.setAttribute('active', '');
    }
    removeActiveItem(item) {
        item.removeAttribute('active');
    }
    toggleOpenState(item) {
        if (item.getAttribute('open') === '') {
            item.removeAttribute('open');
        }
        else {
            item.setAttribute('open', '');
        }
    }
    setAttribute(items, name, value) {
        items.forEach((item) => {
            item.setAttribute(name, value);
        });
    }
    removeAttribute(items, name) {
        items.forEach((item) => {
            item.removeAttribute(name);
        });
    }
    updateBooleanAttribute(items, name, value) {
        items.forEach((item) => {
            if (value) {
                item.setAttribute(name, '');
            }
            else {
                item.removeAttribute(name);
            }
        });
    }
}

export { NavigationUtilities as N };
//# sourceMappingURL=navigation-utilities-D0eHwjII.js.map

//# sourceMappingURL=navigation-utilities-D0eHwjII.js.map