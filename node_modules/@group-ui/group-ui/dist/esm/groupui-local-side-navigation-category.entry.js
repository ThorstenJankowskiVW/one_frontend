import { r as registerInstance, h, H as Host, g as getElement } from './index-DB1NiROX.js';

const localSideNavigationCategoryCss = () => `:host{display:grid;grid-template-columns:100%;grid-area:nav-items;font-family:var(--groupui-sys-font-family-primary), sans-serif;font-weight:var(--groupui-sys-font-weight-bold);font-size:var(--groupui-sys-font-size-350);color:var(--groupui-sys-color-text-weaker);line-height:114%;letter-spacing:var(--groupui-sys-letter-spacing-regular);padding:var(--groupui-sys-spacing-400) var(--groupui-sys-spacing-600) var(--groupui-sys-spacing-400) calc(var(--groupui-sys-spacing-600) + var(--nesting-level) * var(--groupui-sys-spacing-300) + var(--icon-count) * var(--groupui-sys-icon-size-m) + var(--icon-count) * var(--groupui-sys-spacing-200));text-transform:uppercase}:host([size=l]){padding-top:var(--groupui-sys-spacing-600);padding-bottom:var(--groupui-sys-spacing-600)}:host([icon-only]){padding:0}`;

const LocalSideNavigationCategory = class {
    constructor(hostRef) {
        registerInstance(this, hostRef);
        /**
         * hidden attribute
         */
        this.size = 's';
        /**
         * @internal
         */
        this.iconOnly = false;
    }
    componentDidLoad() {
        this.host.style.setProperty('--nesting-level', this.getNestingLevel().level.toString());
        this.host.style.setProperty('--icon-count', this.getNestingLevel().icons.toString());
    }
    getNestingLevel() {
        let level = 0;
        let icons = 0;
        let parent = this.host.parentNode;
        if (!this.hasChildItems() && parent.treeLayout) {
            icons++;
        }
        while (parent != null) {
            if (parent.nodeName === 'GROUPUI-LOCAL-SIDE-NAVIGATION') {
                break;
            }
            if (parent.nodeName === 'GROUPUI-LOCAL-SIDE-NAVIGATION-ITEM') {
                level++;
                const parentItem = parent;
                if (parentItem.icon) {
                    icons++;
                }
            }
            parent = parent.parentNode;
        }
        return { level, icons };
    }
    hasChildItems() {
        let hasItemChild = false;
        this.host.childNodes.forEach((child) => {
            if (child.nodeName === 'GROUPUI-LOCAL-SIDE-NAVIGATION-ITEM' ||
                child.nodeName === 'GROUPUI-LOCAL-SIDE-NAVIGATION-CATEGORY') {
                hasItemChild = true;
            }
        });
        return hasItemChild;
    }
    render() {
        if (this.iconOnly)
            return;
        return (h(Host, null, h("slot", null)));
    }
    get host() { return getElement(this); }
};
LocalSideNavigationCategory.style = localSideNavigationCategoryCss();

export { LocalSideNavigationCategory as groupui_local_side_navigation_category };
//# sourceMappingURL=groupui-local-side-navigation-category.entry.js.map

//# sourceMappingURL=groupui-local-side-navigation-category.entry.js.map